# Nightly summary — 2026-07-23

## Outcome

**Mission**: desktop-overlay  
**Branch**: `opencode/nightly-desktop-overlay-window-guard`

Repository health is green on `codex/design-pixel-launch-experiment`. This run:

- Inspected all desktop overlay code: Electron main process, IPC, preload, 3 renderers
- Tested cross-platform window logic (geometry, state, settings)
- Extracted 5 pure geometry functions into a testable module, added 26 unit tests
- Guarded macOS-only `setHiddenInMissionControl`/`setFullScreenable` behind platform checks
- Fixed two geometry bugs discovered via test-driven analysis

## Repository health

| Check | Result |
|-------|--------|
| `app npm install` | ok |
| `app npm run typecheck` | clean |
| `app npm test` | 57/57 pass |
| `app npm run build` | build ok |

## Recent code inspected

Last 10 commits touch marketing redesign (gradients, pixel theme, light/dark mode, onboarding). No recent changes to `app/src/main/` or `app/src/renderer/`.

## Problems discovered

1. **Unconditional macOS-only API calls** in `windows.ts` and `main.ts`: `setHiddenInMissionControl` and `setFullScreenable` called on all platforms.
2. **Geometry formula bug** in `snap`: bottom-edge snap used `display.height` instead of `bounds.height`.
3. **Bounds migration bug** in `resolveWidgetBounds`: stock old bounds (440×560, 360×480, 340×440) had their position discarded.
4. **Zero test coverage** for window geometry functions.

All 4 problems are fixed in the commit.

## Changes committed

- `app/src/main/windowGeometry.ts` (new, 150 lines) — pure geometry functions
- `app/test/windows.test.ts` (new, 182 lines) — 26 unit tests
- `app/src/main/windows.ts` (modified) — refactored, +platform guards
- `app/src/main/main.ts` (modified) — platform guard on `widget:pin`

## PR status

- PR creation failed: GitHub Actions token lacks `createPullRequest` scope
- Branch pushed: `opencode/nightly-desktop-overlay-window-guard`
- Full PR body prepared in commit message

## Unverified behavior

- **macOS window behavior** (`setHiddenInMissionControl`, `setFullScreenable`, `setAlwaysOnTop` levels) — unverified on Linux runner
- **Multi-display window snapping** — verified in unit tests with mock geometries
- **Electron `safeStorage`** — works on macOS, has fallback on Linux (must unlock keychain)

## Security and privacy concerns

None. No network, credential, or secret changes.

## Required founder decisions

- Approve the `windowGeometry.ts` extraction for ongoing testability
- Confirm platform guard policy (guard macOS only behind `process.platform === 'darwin'`)

## Suggested next daytime task

- Manually verify macOS window behavior (pin, snap, focus, mission control integration)
- Review the branch and merge via GitHub UI
