# Nightly Summary — 2026-07-23

**Mission**: integration-review-and-morning-summary  
**Generated**: 2026-07-23T16:49Z  
**Reviewer**: Unvibe Night Lab (deepseek/deepseek-v4-flash)  
**Previous summary**: `opencode/nightly-integration-review-and-morning-summary-20260723` (2026-07-23T16:03Z)

---

## Repository Health (fresh run)

| Package | typecheck | test | build |
|---------|-----------|------|-------|
| `app/` | clean | 31/31 pass | ok |
| `extension/` | clean | 34/34 pass | ok (49kb) |
| `web/` | **30 errors** (pre-existing) | — | **FAIL** (compile error: `revokeToken` missing on `Store` type) |
| `marketing/` | **5 errors** (pre-existing, missing `@types/node`) | — | — |

**Changes since previous summary**: None. `main` is unchanged. No new branches or commits.

### web/ build failure detail
```
./app/api/v1/auth/signout/route.ts:16:22
Type error: Property 'revokeToken' does not exist on type 'Store'.
```
This is a pre-existing type mismatch where `MemoryStore` has `revokeToken` but the `Store` interface does not declare it. Not introduced by night lab work. `--noEmit` typecheck finds 30 additional errors in `src/data/` and `test/` (billing exports, missing properties on `IncomingEvent`/`ProfileSummary`/`EventRecord`, missing exports from `memoryStore`).

### app/ test note
Current branch runs 31 tests (28 subtests + 3 standalone). Night lab branch `opencode/nightly-desktop-overlay-window-guard` adds 26 new geometry tests (57 total). Those tests only exist on that branch.

---

## Night Lab Branches (4 pushed, 0 merged, 0 PRs)

| Branch | Mission | Report | Changes | Risk |
|--------|---------|--------|---------|------|
| `nightly-desktop-overlay-overview` | desktop-overlay (report) | `2026-07-23-desktop-overlay.md` | `companion.tsx:1373` — `'local'` → `'free'` | Minimal |
| `nightly-desktop-overlay-window-guard` | desktop-overlay (code fix) | `2026-07-23-summary.md` (v1) | `windowGeometry.ts` (new), `windows.test.ts` (new, 26 tests), `windows.ts`, `main.ts` | Low |
| `nightly-product-design-and-accessibility-plan-page-a11y` | product-design-and-a11y | **None** (code only) | `companion.tsx` (PlanSkeleton, aria, radiogroup), `companion.css` (skeleton animation, reduced-motion) | Low |
| `nightly-integration-review-and-morning-summary-20260723` | integration-review | `2026-07-23-summary.md` (v2) | Summary only | None |

### Gap: missing report for product-design branch
The `nightly-product-design-and-accessibility-plan-page-a11y` branch has no report file in `docs/`. All other night lab branches include one. Consider adding a brief report documenting the skeleton loader and a11y improvements.

---

## Overlap Analysis

| File | Branches | Conflict | Notes |
|------|----------|----------|-------|
| `companion.tsx` | overview + a11y | Auto-mergeable | Different sections (Plan page vs UsageChip label) |
| `companion.css` | a11y only | None | — |
| `windows.ts`, `main.ts`, `windowGeometry.ts` | window-guard only | None | — |
| `2026-07-23-summary.md` | window-guard + integration-review | Overwrite | Integration review supersedes v1 from window-guard |

No incompatible recommendations. All branches modify independent sections.

---

## Problems Discovered & Fixed

| # | Problem | Branch | Risk | Status |
|---|---------|--------|------|--------|
| 1 | macOS-only `setHiddenInMissionControl`/`setFullScreenable` called unconditionally | window-guard | Low | Fixed |
| 2 | Geometry snap bug: bottom-edge used display height instead of bounds height | window-guard | Low | Fixed |
| 3 | Bounds migration bug: old stock bounds had position discarded | window-guard | Low | Fixed |
| 4 | Zero test coverage for window geometry | window-guard | Low | Fixed (26 tests) |
| 5 | Fallback plan label `'local'` not recognized | overview | Minimal | Fixed |
| 6 | Plan page missing loading/empty/error states | a11y | Low | Fixed (skeleton + aria) |
| 7 | Plan toggle missing `role=radiogroup`, progress bars missing `aria-label` | a11y | Low | Fixed |

---

## Risky Code & macOS Requirements

All macOS-specific behavior is guarded by `process.platform === 'darwin'` in `window-guard`. Unverified on Linux runner:

- `setHiddenInMissionControl(true)` — `windows.ts`
- `setFullScreenable(false)` — `windows.ts`
- `setAlwaysOnTop` level behavior — `windows.ts`
- `titleBarStyle: 'hiddenInset'` — ignored on Linux
- `systemPreferences.isTrustedAccessibilityClient()` — returns `true` on Linux without prompt
- `app.dock` — already guarded
- `safeStorage` — macOS keychain vs Linux fallback

---

## Recommended Review & Merge Order

1. **`nightly-desktop-overlay-window-guard`** — Platform safety + test coverage (highest value)
2. **`nightly-product-design-and-accessibility-plan-page-a11y`** — Loading states + a11y + reduced-motion
3. **`nightly-desktop-overlay-overview`** — Simple label fix (can merge any time)

All three can merge independently (no file conflicts). Merge via GitHub UI.

---

## Security & Privacy

No secrets, credentials, or network changes introduced. No backend code modified. Secret filtering preserved. No `.env` or API keys committed.

---

## Required Founder Decisions

1. **Approve `windowGeometry.ts` extraction** for ongoing testability (window-guard branch)
2. **Confirm platform guard policy**: guard macOS-only APIs behind `process.platform === 'darwin'`
3. **Manually verify macOS window behavior**: pin, snap, focus, Mission Control, full-screenable
4. **Open PRs**: GitHub Actions token lacks `createPullRequest` scope. Links:
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-desktop-overlay-window-guard
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-product-design-and-accessibility-plan-page-a11y
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-desktop-overlay-overview
5. **Consider adding a report file** for the product-design-and-accessibility branch
6. **Address web/ build failure** — `revokeToken` missing on `Store` interface (pre-existing, blocks CI)

---

## Suggested Next Daytime Task

1. Manually verify macOS window behavior (pin, snap, focus, Mission Control)
2. Review and merge the three night lab branches via GitHub UI
3. Address the web/ build failure (`Store` interface missing `revokeToken`)
