# Nightly Summary — 2026-07-23

**Mission**: integration-review-and-morning-summary  
**Generated**: 2026-07-23T16:03Z  
**Reviewer**: Unvibe Night Lab (deepseek/deepseek-v4-flash)

---

## Repository Health

| Package | typecheck | test | build |
|---------|-----------|------|-------|
| `app/` | clean | 31/31 pass | ok |
| `extension/` | 6 errors (pre-existing: VS Code types) | — | build err (pre-existing) |
| `web/` | 10 errors (pre-existing: `@types/node`) | — | — |

**Summary**: `app/` is green. Extension and web have pre-existing type errors unrelated to night lab work.

---

## Night Lab Branches (3 pushed, 0 merged, 0 PRs)

### 1. `opencode/nightly-desktop-overlay-overview`
**Mission**: desktop-overlay (report + label fix)  
**Report**: `docs/automation/nightly/2026-07-23-desktop-overlay.md`  
**Changes**: `companion.tsx:1373` — `plan: 'local'` → `plan: 'free'`  

### 2. `opencode/nightly-desktop-overlay-window-guard`
**Mission**: desktop-overlay (code fix)  
**Summary**: `docs/automation/nightly/2026-07-23-summary.md` (first version)  
**Changes**: `windowGeometry.ts` (new, 94 lines), `windows.test.ts` (new, 196 lines, 26 tests), `windows.ts` (refactored + macOS guards), `main.ts` (platform guard)  

### 3. `opencode/nightly-product-design-and-accessibility-plan-page-a11y`
**Mission**: product-design-and-accessibility (a11y fix)  
**Report**: None (code only)  
**Changes**: `companion.tsx` (PlanSkeleton loading state, `aria-label`, `role=radiogroup`), `companion.css` (skeleton animation, `prefers-reduced-motion`)  

---

## Overlap Analysis

| File | Branches | Conflict | Notes |
|------|----------|----------|-------|
| `companion.tsx` | 1 + 3 | Auto-mergeable | Different sections (line 970 vs 1373) |
| `companion.css` | 3 only | None | — |
| `windows.ts`, `main.ts`, `windowGeometry.ts` | 2 only | None | — |

No incompatible recommendations across branches.

---

## Problems Discovered & Fixed

| # | Problem | Branch | Risk |
|---|---------|--------|------|
| 1 | macOS-only `setHiddenInMissionControl`/`setFullScreenable` called unconditionally | window-guard | Low |
| 2 | Geometry snap bug: bottom-edge used display height instead of bounds height | window-guard | Low |
| 3 | Bounds migration bug: old stock bounds had position discarded | window-guard | Low |
| 4 | Zero test coverage for window geometry | window-guard | Low |
| 5 | Fallback plan label `'local'` not recognized by UsageChip display logic | overview | Minimal |

---

## Risky Code & macOS Requirements

All macOS-specific behavior is correctly guarded by `process.platform === 'darwin'` in the `window-guard` branch. Items unverified on Linux runner:

- `setHiddenInMissionControl(true)` — `windows.ts:98`, `windows.ts:196`
- `setFullScreenable(false)` — `windows.ts:99`, `windows.ts:197`
- `setAlwaysOnTop` level behavior — `windows.ts:96`, `windows.ts:193`
- `titleBarStyle: 'hiddenInset'` — `windows.ts:305` (ignored on Linux)
- `systemPreferences.isTrustedAccessibilityClient()` — returns `true` on Linux without prompt
- `app.dock` — already guarded
- `safeStorage` — macOS keychain vs Linux fallback

---

## Recommended Review & Merge Order

1. **`opencode/nightly-desktop-overlay-window-guard`** — Platform safety + test coverage
2. **`opencode/nightly-product-design-and-accessibility-plan-page-a11y`** — Loading states + a11y
3. **`opencode/nightly-desktop-overlay-overview`** — Simple label fix (can merge in any order)

All three can be merged independently (no conflicts).

---

## Security & Privacy

No secrets, credentials, or network changes introduced. No backend code modified. Secret filtering preserved.

---

## Required Founder Decisions

1. **Approve `windowGeometry.ts` extraction** for ongoing testability
2. **Confirm platform guard policy**: guard macOS-only APIs behind `process.platform === 'darwin'`
3. **Manually verify macOS window behavior**: pin, snap, focus, Mission Control, full-screenable
4. **Open PRs**: GitHub Actions token lacks `createPullRequest` scope. Manual PR creation needed:
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-desktop-overlay-window-guard
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-product-design-and-accessibility-plan-page-a11y
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-desktop-overlay-overview

---

## Suggested Next Daytime Task

Manually verify macOS window behavior (pin, snap, focus, Mission Control integration), review the three branches, and merge via GitHub UI.
