# Nightly summary — 2026-08-08

Integration-review mission. Inspected every night-lab branch and report from
tonight (08-08), test-merged all five onto a fresh `main`, re-ran repository
checks, and re-verified both competing web-fix carriers in clean worktrees.
Nothing was merged.

## Repository health (verified fresh this review, on a test-merge of all five branches)

| Package | Typecheck | Test | Build |
|---------|-----------|------|-------|
| `app/` | **pass** | **42/42 pass** | **pass** |
| `extension/` | **pass** | **34/34 pass** | n/a (parked) |
| `marketing/` | **pass** | **19/19 pass** | **pass** |
| `web/` | **FAIL — 8 errors** | **31/36 (5 fail)** | **pass** |

`web/` on `main` remains red with the **same pre-existing breakage tracked
every night since 2026-07-29**. No new commits landed on `main` since
`bbb1dfc`; all recent `main` changes are confined to `marketing/` (waitlist
attribution scorecard, copy/rollback). The complete fix is verified and
sitting unmerged (see "Conflicting recommendations" below).

## Tonight's runs (2026-08-08) — 5 branches, all pushed, none merged

| Mission | Branch | Change | Fresh verification (this review) |
|---------|--------|--------|-----------------------------------|
| repository-health | `nightly-repository-health-20260808` | gitDiff deleted-file path fix + `web/.gitignore` `*.tsbuildinfo` + lockfile version sync | app 42/42 incl. regression test; web hygiene ok |
| backend-and-sync | `nightly-backend-and-sync-event-batch-wedge` | `batchEvents` uploads ≤400/batch under backend 500 cap (fixes permanent sync wedge) | app 42/42 incl. 2 new tests |
| ai-learning-engine | `nightly-ai-learning-engine-comprehension-guard` | `isValidComprehensionQuestion` guard at app trust boundary (studyQuiz + review) | app 42/42 incl. 5 new protocol tests |
| product-design-and-accessibility | `nightly-product-design-and-accessibility-widget-quiz-radiogroup` | widget "Test me" answers → `role=radio` roving-tabindex radiogroup + `role=status` verdict | app 42/42 incl. 4 new quizPicker tests |
| competitor-research-and-v2 | `nightly-competitor-research-and-v2-citation-grounding` | Research report: citation/grounding UX | docs-only, no code |

All five branches **merge onto fresh `main` with zero conflicts** (verified
this review). Three open PRs exist but are from claude/codex agents, not the
night lab; night-lab PR creation is still blocked (below).

## Overlapping files / incompatible recommendations

1. **`app/src/core/gitDiff.ts` — tonight's fix duplicates the 08-03 fix.**
   `nightly-auto-repository-health-20260803` already fixed the same deleted-file
   `/dev/null` hunk-label bug (different implementation: skips `/dev/null` on
   `+++`/`---`; tonight's: tracks `oldFile` and falls back when `+++` is
   `/dev/null`). Both are unmerged. Merge **one** — tonight's
   `nightly-repository-health-20260808` is the better-shaped fix (explicit
   `oldFile` tracking, one regression test). Do not merge both.
2. **Web-fix carrier contradiction (must resolve).** The 08-08 health report
   names `nightly-backend-and-sync-web-contract-alignment` (08-02) as "the
   verified fix" and recommends merging it. The 08-07 integration summary
   explicitly recommended **against** it. I re-verified both in clean
   worktrees:
   - `web-contract-alignment`: typecheck clean, **37/37 pass** — but it
     **re-adds** `priceFor`/`proAnnualSavingsPercent`/`teamsAnnualSavingsPercent`
     exports that `main` deliberately dropped (`eddac36`), conflicting with
     `docs/billing.md:80` ("Legacy Teams seat math remains in tests").
   - `session-ttl-parity` (08-07): typecheck clean, **35/35 pass** — drops the
     stale pricing test instead, restores the dev-store device-code contract,
     and enforces 30-day session expiry. `docs/billing.md` corroborates this
     approach.
   - The two **directly conflict** on `web/src/billing/plans.ts` and
     `web/src/data/memoryStore.ts` (verified: content conflicts on merge).
   - **Recommendation: merge `session-ttl-parity`; retire `web-contract-alignment`.**
     `session-ttl-parity` also subsumes `stale-tests-20260807` (verified
     ancestor chain).
3. **`app/src/renderer/widget/widget.tsx` keeps accumulating branches.** Now
   three pending branches edit it: 08-06 `bar-placement-and-widget-keys`,
   08-07 `widget-depth-radiogroup`, 08-08 `widget-quiz-radiogroup`. They are
   additive in disjoint regions (confirmed clean 08-07 test-merge); the only
   friction is one shared import line and a second `useRef` next to `bodyRef`.
   Merge in date order (08-06 → 08-07 → 08-08) and reconcile once.
4. **`app/package-lock.json` version sync** (0.1.1 → 0.1.2) appears in nearly
   every branch — cosmetic, all identical, resolves cleanly.

## Risky code / macOS testing requirements

- **app-side keyboard/focus a11y (08-08 quiz radiogroup; 08-07 depth
  radiogroup; 08-06 widget keys):** unit-tested pure helpers, but live
  keyboard/focus/VoiceOver behavior inside a real Electron window is
  **unverified on the Linux runner**. Each needs a short real-Mac pass before
  merge.
- **`session-ttl-parity` adds a `SupabaseStore.userForToken` expiry filter** —
  static-inspected + typechecked only (no credentials here). Must be exercised
  by the guarded `npm run verify:staging` release gate.
- **comprehension-guard (08-08):** logic unit-tested; real-AI path unexercised
  (no `ANTHROPIC_API_KEY`). **mock AI — real key required for verification**.
- **No secrets in renderer.** Re-verified this review: no SUPABASE /
  service-role key reaches `app/src/renderer/` or `app/src/preload/`;
  `SUPABASE_URL`/`SERVICE_ROLE_KEY` load only in `web/` server code.

## Tests run (fresh, this review)

- `app` (all five branches merged onto `main`): typecheck pass; **42/42 pass**; build ok.
- `extension` (`main`): typecheck pass; **34/34 pass**.
- `marketing` (`main`): typecheck pass; **19/19 pass**; build ok.
- `web` (`main`): **8 typecheck errors + 5 test failures** (pre-existing).
- `session-ttl-parity` (clean worktree): web typecheck clean; **35/35 pass**.
- `web-contract-alignment` (clean worktree): web typecheck clean; **37/37 pass** (but see carrier recommendation).

## Failures

- `web/` on `main` still fails typecheck + 5 tests. The correct complete fix
  (`session-ttl-parity`) has been ready since 08-07, unmerged.
- Night-lab PR creation remains blocked (below) — tonight's five branches have
  no PRs; the 08-07 summary also has no PR.

## Security or privacy concerns

None new. Tonight's app changes slightly improve the privacy surface
(deleted-file hunks now carry the real path during the pre-send secret scan).
No secrets touched; renderer holds no network/FS access; no Supabase
service-role key in any branch.

## Required founder decisions

1. **Unblock night-lab PR creation** (persistent since 2026-07-24): enable
   "Allow GitHub Actions to create and approve pull requests" (or run the
   night lab with a PAT with `pull-requests: write`). Branches push fine but
   `gh pr create` 403s. **10+ branches are currently without PRs**.
2. **Resolve the web-fix carrier:** merge `session-ttl-parity` (makes `web/`
   green, enforces session expiry, subsumes `stale-tests-20260807`) and
   **retire** `web-contract-alignment` (08-02) rather than merge it — it
   restores exports `main` deliberately removed and conflicts with
   `session-ttl-parity`. The 08-08 health report's suggestion to merge
   `web-contract-alignment` should be overridden.
3. **macOS verification pass** for the widget a11y branches (08-06/08-07/08-08)
   before merging, or merge-with-unit-tests-and-defer the Mac pass.
4. **Retire `nightly-auto-repository-health-20260803`** — its gitDiff fix is
   superseded by tonight's better-shaped fix on
   `nightly-repository-health-20260808`.

## Recommended review order

1. `nightly-backend-and-sync-session-ttl-parity` (unblocks `web/`; supersedes
   `stale-tests-20260807`; superseded-carrier decision above).
2. `nightly-backend-and-sync-event-batch-wedge` (08-08; app sync fix, tested).
3. `nightly-ai-learning-engine-comprehension-guard` (08-08; app trust-boundary guard, tested).
4. `nightly-repository-health-20260808` (08-08; gitDiff fix + hygiene).
5. `nightly-desktop-overlay-bar-placement-and-widget-keys` (08-06).
6. `nightly-product-design-and-accessibility-widget-depth-radiogroup` (08-07).
7. `nightly-product-design-and-accessibility-widget-quiz-radiogroup` (08-08).
8. `nightly-competitor-research-and-v2-citation-grounding` (08-08; research, low priority).

## Recommended merge order

1. `session-ttl-parity` — carrier; makes `web/` green.
2. `event-batch-wedge` (app; independent of widget.tsx).
3. `ai-learning-engine-comprehension-guard` (app; independent).
4. `repository-health-20260808` (gitDiff fix).
5. `bar-placement-and-widget-keys` (08-06) → `widget-depth-radiogroup` (08-07)
   → `widget-quiz-radiogroup` (08-08) — **in this order** to keep
   `widget.tsx` reconciliation to a single import-line pass.
6. **Skip/close** `web-contract-alignment` (08-02),
   `auto-repository-health-20260803` (08-03, gitDiff duplicate), and any
   08-05 `bar-placement-toggle-off` twin (superseded by 08-06).

## Suggested next daytime task

Unblock night-lab PR creation; merge `session-ttl-parity` and the four
08-08 app branches; then a short macOS keyboard/focus/voiceover pass on the
three widget.tsx branches before merging them. Re-open the web-fix decision
only if a founder prefers re-adding the removed pricing exports (documented
as legacy in `docs/billing.md`).

---

## Addendum — PR-creation attempt (2026-08-08)

The summary branch
`opencode/nightly-integration-review-and-morning-summary-20260808` is pushed to
`origin`. `gh pr create` is expected to fail with the persistent blocker
(`GitHub Actions is not permitted to create or approve pull requests`); open
the PR manually from
https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-integration-review-and-morning-summary-20260808
targeting `main`.
