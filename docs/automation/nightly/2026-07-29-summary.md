# Nightly summary — 2026-07-29

## Outcome

Night-lab run from 2026-07-29. Two branches were found: `opencode/nightly-auto-repository-health-20260729` (created first) and `opencode/nightly-integration-review-and-morning-summary-20260729` (created later). Both contain only the summary file; the latter is a superset refinement of the former. No PRs were opened for either.

This verification run (`verify-20260729`) re-ran all health checks and corrected the summary.

**NIGHT_LAB variables**: all unset — defaulting to no code changes, no research, max risk = low.

## Repository health

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | pass | 31/31 pass | pass |
| `extension/` | ok | pass | 34/34 pass | n/a (not required) |
| `web/` | ok | **FAIL (8 errors)** | 31/36 pass (**5 fail**) | pass |
| `server/` | ok | pass | no test script | no build script |
| `marketing/` | ok | pass | 18/18 pass | pass |

### web/ typecheck errors (8 errors, test files only)

- `test/billing.test.ts:13-15` — imports `priceFor`, `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent` from `../src/billing/plans` — none exist there now
- `test/billing.test.ts:47-48` — `planLimit('pro', 'ai_explanation')` needs 3 args, gets 2
- `test/security.test.ts:3` — imports `SESSION_TTL_MS` from `../src/data/memoryStore` — not exported
- `test/security.test.ts:53,62` — `new MemoryStore(() => now)` — constructor takes 0 args

Cause: source was refactored (pricing simplified, MemoryStore constructor changed); tests not fully aligned.

### web/ test failures (5 of 36)

All 5 failures are in `billing.test.ts` and `security.test.ts`, matching the typecheck errors above — tests crash at import/construction time rather than producing false logic results.

## Known audit issues (carried forward)

- **Device code polling** has no progressive backoff (`main.ts:910-924`).
- **No server-side rate limiting** on any API route.
- **Preload listener methods** (5 of 8) don't return cleanup functions (`preload.ts:55,56,70,87,94`).
- **Hardcoded macOS path** in `backend.ts:19` (`~/Library/Application Support`).
- **Missing token patterns** in secret filter (npm, GitLab, Discord, etc.).
- **No `web/src/ai/` tests** — comprehension parsing, prompt building untested.
- **Model defaults** (`claude-sonnet-5`, `gemini-3.1-flash-lite`) reference future/fictional model names.

### Correction note
Prior summary from `integration-review-and-morning-summary-20260729` reported 5 typecheck errors for web/. Actual count is 8. The `auto-repository-health-20260729` summary correctly reported 8.

## Recent commits inspected

`git log --oneline -10` on `main`:
```
af566ca Frame Unvibe as vibe-code learning
df886d2 Point homepage demo to latest official video
f8d823a Merge pull request #10 from ShadowEsu/feat/link-new-demo-video
43b2c25 Link homepage demo to latest YouTube video
eb5b4aa Merge pull request #9 from ShadowEsu/fix/waitlist-dashboard-auth
eb83d63 Require founder token in waitlist dashboard
1ec2679 Merge pull request #8 from ShadowEsu/hotfix/waitlist-admin-auth
2f725eb Fix waitlist admin authorization
e0e053d Restyle Google device approval for Unvibe
eddac36 Restore web auth and billing build compatibility
```

Recent activity is marketing-focused (branding, waitlist, auth fixes). No core architectural changes in the last 10 commits.

## Branch overlap analysis

### Two branches, same file
Both `nightly-auto-repository-health-20260729` and `nightly-integration-review-and-morning-summary-20260729` modify only `docs/automation/nightly/2026-07-29-summary.md`. They conflict in content — the health branch (106 lines) and the review branch (129 lines) have different structures and slightly different error counts. The review branch version is more complete.

### No conflicting recommendations
Both branches agree on all findings. No incompatible recommendations across any nightly branches from tonight.

### Missing verification
- The 5 test failures in web/ were not re-verified after the branch commits — re-run here and confirmed.
- No competitor research was conducted (NIGHT_LAB_ALLOW_RESEARCH is unset).
- No code changes were attempted (NIGHT_LAB_ALLOW_CODE_CHANGES is unset).

### Unverified on Linux runner
- `osascript`-based selection capture (`selection.ts`) — returns null gracefully on Linux.
- `systemPreferences.isTrustedAccessibilityClient` — macOS-only, properly guarded.
- `app.dock` / template images — macOS-only, guarded.
- `titleBarStyle: 'hiddenInset'` — macOS-only, falls through on Linux.

## Recommended review order

1. Fix stale web test fixtures (`test/billing.test.ts`, `test/security.test.ts`) — quick, low-risk, unblocks CI.
2. Add cleanup functions to preload listeners — contained fix, improves reliability.
3. Review device code polling backoff and server-side rate limiting — pre-PR hardening.
4. Schedule `web/src/ai/` test coverage — not urgent but prevents regressions.
5. Update model defaults to real published model names on next dependency bump.

## Deferred (requires founder decision)

- Expanding secret filter token patterns — scope/schedule decision needed.
- macOS path fallback for Linux — blocked by desktop pivot (Linux not yet primary target).
- Dependency vulnerability audit (`app/`: 29 high/critical in transitive deps; `web/`: 2 high) — requires controlled upgrade plan.
- Merge/clean up duplicate nightly branches — upstream should squash or close `nightly-auto-repository-health-20260729` in favor of `nightly-integration-review-and-morning-summary-20260729`.

## Security and privacy

- **No keys found in renderer code** — all network I/O is main-process-only.
- **Supabase service-role key**: server-side only, never in `app/`.
- **Bearer tokens**: encrypted with `safeStorage` at rest.
- **Secret filter**: active, blocks credentials before remote requests.
- **Mock AI**: clearly labeled at every output layer.

## Required founder decisions

1. Accept the deferred `web/` test alignment as known tech debt, or prioritize a fix.
2. Decide whether to expand the secret filter's known token pattern list.
3. Approve or reject the future/fictional model name defaults — when should they map to real deployed model IDs?
4. Clean up duplicate nightly branches (`auto-repository-health` vs `integration-review`) — squash or delete one.
5. Consider setting `NIGHT_LAB_ALLOW_CODE_CHANGES=true` for low-risk fixture fixes and `NIGHT_LAB_ALLOW_RESEARCH=true` for automated research.

## Suggested next daytime tasks

1. Fix stale web test fixtures (billing.test.ts, security.test.ts) — ~20 min
2. Fix preload listener cleanup — ~15 min
3. Review and merge outstanding non-night-lab PRs (#5, #6, #7)
4. Consolidate duplicate nightly branches
5. Set NIGHT_LAB variables to enable more autonomous nightly work
