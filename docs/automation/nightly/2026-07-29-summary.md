# Nightly summary — 2026-07-29

## Run context

- **Mission**: Integration review & morning summary
- **NIGHT_LAB_ALLOW_CODE_CHANGES**: unset (default: no code changes)
- **NIGHT_LAB_ALLOW_RESEARCH**: unset (default: no research)
- **NIGHT_LAB_MAX_RISK**: unset (default: low)
- **This run**: reconciles 3 nightly branches created tonight, all touching only this file

## Branches created tonight

| Branch | Created | Content | Status |
|--------|---------|---------|--------|
| `nightly-auto-repository-health-20260729` | First | 106-line summary | Reported 8 typecheck errors (correct) |
| `nightly-integration-review-and-morning-summary-20260729` | Second | 129-line summary | Reported 5 typecheck errors (incorrect — actual: 8) |
| `nightly-integration-review-and-morning-summary-verify-20260729` | Third | 121-line summary | Corrected to 8 errors. Most complete prior version. |

**Recommendation**: Squash these 3 branches. The verify branch has the most accurate data. This summary replaces all three.

## Repository health (verified fresh at 2026-07-30 ~16:51 UTC)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **31/31 pass** | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | n/a |
| `web/` | ok | **FAIL (8 errors)** | **31/36 pass (5 fail)** | **pass** |
| `server/` | ok | **pass** | no test script | n/a |
| `marketing/` | ok | **pass** | **18/18 pass** | **pass** |

### web/ typecheck errors (8, all in test files)

- `test/billing.test.ts:13-15` — imports `priceFor`, `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent` from `../src/billing/plans` — none exported there
- `test/billing.test.ts:47-48` — `planLimit('pro', 'ai_explanation')` needs 3 args, gets 2
- `test/security.test.ts:3` — imports `SESSION_TTL_MS` from `../src/data/memoryStore` — not exported
- `test/security.test.ts:53,62` — `new MemoryStore(() => now)` — constructor takes 0 args

**Root cause**: Source refactored (pricing simplified, MemoryStore constructor changed); tests not aligned.

### web/ test failures (5 of 36)

1. **billing.test.ts:23** — `priceFor is not a function` (import error, crash)
2. **billing.test.ts:39** — `Missing expected exception` (assertion failure)
3. **security.test.ts:45** — duplicate device approval: returns new UUID instead of existing one
4. **security.test.ts:56** — expired device code: returns UUID instead of `null`
5. **security.test.ts:66** — expired session: returns UUID instead of `null`

## Known audit issues (carried forward, verified tonight)

### Backend (web/)
- `redeemDeviceCode` return type mismatch: interface says `'used' | 'expired'`, neither MemoryStore nor SupabaseStore returns these — they return `'pending'` or `'unknown'`.
- MemoryStore has no session/device-code expiry — tokens live forever, no time-based eviction.
- No server-side rate limiting on any API route.
- No prompt versioning — server and local prompts diverge.
- Model defaults (`claude-sonnet-5`, `gemini-3.1-flash-lite`) reference future/fictional model names.
- No retry/backoff on upstream AI API failures.

### Desktop overlay (app/)
- Preload listener leak: `onReviewEvent` and `onAutocollapse` (`preload.ts:55-56`) use `ipcRenderer.on` without returning unsubscribe functions.
- Widget IPC listener leak: `widget.tsx:271` `useEffect` registers listeners with no cleanup.
- `Icon` component (`companion.tsx:144`): SVG rendered without `aria-hidden="true"`.
- Settings modal: No `Escape`-to-close handler (backdrop click only).
- Hardcoded macOS path in `backend.ts:19` (`~/Library/Application Support/Unvibe/.env`).

### AI learning engine
- Three stale protocol copies: extension (3 levels), app and web (5 levels) — no shared package.
- No controlled concept vocabulary: model can invent any kebab-case slug.
- Skill derivation: heuristic (3 correct = "Strong") with no calibration data.
- Unguarded `JSON.parse` on model output.

### Marketing
- No issues found. All 18 tests pass, build and typecheck clean.

## Recent commits on main

```
af566ca Frame Unvibe as vibe-code learning
df886d2 Point homepage demo to latest official video
f8d823a Merge pull request #10 (feat/link-new-demo-video)
43b2c25 Link homepage demo to latest YouTube video
eb5b4aa Merge pull request #9 (fix/waitlist-dashboard-auth)
eb83d63 Require founder token in waitlist dashboard
```

Recent activity is marketing/auth focused. No core architectural changes.

## Security & privacy

- No credentials or API keys in tracked files.
- Secret filtering runs on-device before any network call (verified in `review.ts`).
- Server-side credentials (`SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`) properly isolated.
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` correctly client-side only (Supabase RLS protects data).
- Comprehension answer key stays in main process until user submits.

## Unverified on Linux runner

- `osascript`-based selection capture (`selection.ts`) — returns null gracefully on Linux.
- `systemPreferences.isTrustedAccessibilityClient` — macOS-only, guarded.
- `app.dock` / template images — macOS-only, guarded.
- `titleBarStyle: 'hiddenInset'` — macOS-only, falls through on Linux.

## PR and research status

- **No night-lab PRs open** — the 3 nightly branches have no PRs.
- **3 non-night-lab PRs** open (#5, #6, #7) — unrelated to nightly automation.
- **No competitor research** conducted tonight (`NIGHT_LAB_ALLOW_RESEARCH` unset).

## Required founder decisions

1. **Stale web test fixtures**: Fix `test/billing.test.ts` and `test/security.test.ts` to match refactored source (~30 min). Accept as known tech debt until then.
2. **MemoryStore expiry**: Add session/device-code expiry to MemoryStore for dev/prod alignment.
3. **Preload listener cleanup**: Add unsubscribe returns to `onReviewEvent`/`onAutocollapse` — low risk, improves reliability.
4. **Clean up duplicate nightly branches**: Squash/delete `nightly-auto-repository-health-20260729` and `nightly-integration-review-and-morning-summary-20260729`; keep verify version.
5. **Set NIGHT_LAB env vars**: To enable auto-fixes and research, set `NIGHT_LAB_ALLOW_CODE_CHANGES=true` and/or `NIGHT_LAB_ALLOW_RESEARCH=true`.
6. **Model defaults**: Map `claude-sonnet-5`, `gemini-3.1-flash-lite` to real deployed model IDs.

## Recommended review & merge order

1. Fix stale web test fixtures (quick, low-risk, unblocks CI)
2. Add cleanup functions to preload listeners (`preload.ts:55-56`)
3. Add `aria-hidden` to Icon component (`companion.tsx:144`)
4. Add `Escape` handler to Settings modal
5. Review MemoryStore expiry alignment
6. Schedule `web/src/ai/` test coverage

## Deferred (requires founder decision)

- Expanding secret filter token patterns — scope/schedule decision
- macOS path fallback for Linux — blocked by platform target decision
- Dependency vulnerability audit (`app/`: 29 high/critical transitive) — requires controlled upgrade plan
- Controlled concept vocabulary — v2 concern
- Prompt versioning — v2 concern
- Rate limiting — pre-public-launch hardening

## Suggested next daytime tasks

1. Fix stale web test fixtures (~20 min)
2. Fix preload listener cleanup (~15 min)
3. Review and merge non-night-lab PRs (#5, #6, #7)
4. Consolidate duplicate nightly branches
5. Set NIGHT_LAB env vars for more autonomous nightly runs
