# Nightly summary — 2026-08-05

Integration-review mission. Inspected tonight's 3 pushed branches, test-merged
them onto `main` fresh, re-ran repository checks, and checked overlap against
the stale unmerged backlog. Nothing was merged.

## Repository health (verified fresh this review)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **31/31 pass** | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | n/a (parked) |
| `web/` | ok | **FAIL — 8 errors** | **31/36 pass (5 fail)** | **pass** |
| `marketing/` | ok | **pass** | **19/19 pass** | **pass** |

`web/` on `main` is still red with the **same pre-existing breakage flagged
every night since 2026-07-29** (missing `priceFor`,
`proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`, `SESSION_TTL_MS`;
wrong `planLimit`/`MemoryStore` arity) plus the **same 5 failing tests**.
Re-verified fresh: the complete fix on the unmerged
`nightly-backend-and-sync-web-contract-alignment` branch is still clean
(**typecheck pass, 37/37 pass** on that branch) and merges onto `main` with
0 conflicts. It remains unmerged and still has no PR.

## Tonight's runs (2026-08-05)

Three of seven scheduled missions produced pushed branches. **repository-health
(10:17), backend-and-sync (11:17) did not run** (same as 08-04; GitHub schedule
delivery continues to skip those slots — on 08-04 they finally fired ~6h late).
**ai-learning-engine (13:02) was cancelled after hitting the 15-minute
`timeout-minutes`** (recurring: 08-02, 08-03, and now 08-05).

| Mission | Run | Result |
|---------|-----|--------|
| desktop-overlay | 12:18 | `barPlacement` toggle-off fix — **pushed** |
| ai-learning-engine | 13:02 | **CANCELLED — 15-min timeout** (no branch) |
| competitor-research-and-v2 | 14:34 | Research report (metacognition) — **pushed** |
| product-design-and-accessibility | 15:25 | Quiz-mode radiogroup keyboard a11y — **pushed** |
| integration-review (this mission) | 16:12 | in progress |

## Branches opened tonight (3, all pushed)

| Branch | Change | Verification (fresh, this review) |
|--------|--------|-------------------------------------|
| `nightly-desktop-overlay-bar-placement-toggle-off` | New `app/src/core/barPlacement.ts` + `main.ts` wiring so "Follow active display" toggling OFF repositions the bar (fix was lost 08-04, re-applied) +5 tests | app typecheck pass; tests pass; build ok |
| `nightly-competitor-research-and-v2-metacognition-calibration` | Research + founder-decisions docs only (no code) | docs-only; no code impact |
| `nightly-product-design-and-accessibility-quiz-radiogroup-keyboard` | New `app/src/core/rovingRadio.ts` + `companion.tsx` Quiz mode bar: roving tabindex + Arrow/Home/End nav; lockfile version normalized 0.1.1→0.1.2 +6 tests | app typecheck pass; tests pass; build ok |

**Test-merge of all 3 onto `main`: 0 conflicts.** Merged state re-checked fresh:
app typecheck pass, **42/42 tests pass** (31 main + 5 barPlacement + 6
rovingRadio), build ok. The branches are disjoint and individually verified.

## Overlapping files / incompatible recommendations

1. **Tonight's 3 branches are disjoint.** Desktop-overlay touches `main.ts` +
   new `barPlacement.ts`; PDA touches `companion.tsx` + new `rovingRadio.ts`;
   research is docs-only. Test-merge of all three onto `main`: **0 conflicts**.
2. **`companion.tsx` pile-up continues.** 18 branches total now edit
   `app/src/renderer/companion/companion.tsx` (all the PDA a11y branches +
   several desktop-overlay ones). Tonight's PDA branch is the latest. Two
   recent stale branches conflict with it on that file (test-merged):
   `nightly-product-design-and-accessibility-companion-focus-states` and
   `nightly-product-design-and-accessibility-history-filter-tabs` both
   **conflict on companion.tsx**. Do **not** batch-merge these; reconcile one
   per day.
3. **`main.ts` is fine.** Tonight's bar-placement edit does **not** conflict
   with the stale `nightly-desktop-overlay-window-guard` or
   `nightly-desktop-overlay-overview` branches (both test-merged clean).
4. **web/ carrier still the unblocker.** `nightly-backend-and-sync-web-contract-alignment`
   merges clean, typechecks, 37/37 pass. Nothing tonight touches
   `web/src/billing/` or `web/src/data/`, so merging the carrier first stays safe.
5. **ALE branches unchanged.** No ALE run tonight (timeout). The stale ALE
   branches (e.g. `nightly-ai-learning-engine-comprehension-task-fix`) merge
   clean against tonight's combined state — test-verified.

## Risky code / macOS testing requirements

- **PDA roving-radio keyboard nav** (Arrow/Home/End + focus move in a live
  window) — logic unit-covered; real-keyboard behavior is **unverified on
  Linux runner**. Requires a real-Mac pass.
- **desktop-overlay bar placement** — the whole "follow active display"
  geometry path is **unverified on Linux runner** (macOS multi-display);
  unit-covered decision logic only.
- **Metacognition research** — no code; claims rest on cited public sources
  (fetched 2026-08-05). Any resulting "Test me" redesign stays **mock AI — real
  key required for verification**.
- **web-contract-alignment** (unmerged) restores security semantics (10-min
  device-code TTL, one-time redemption, 30-day opaque-session expiry) in
  `MemoryStore`; `SupabaseStore` remains unverified (no credentials).

## Tests run (fresh, this review)

- `app` (main + all 3 pushed branches test-merged): typecheck pass;
  **42/42 pass**; build ok.
- `web` (main + carrier branch): main = 8 typecheck errors + 5 test failures
  (pre-existing); carrier branch = typecheck clean, **37/37 pass**; build ok.
- `marketing` main: typecheck pass; **19/19 pass**; build ok.
- `extension` main: typecheck pass; **34/34 pass**.

## Failures

- `web/` on `main` still fails typecheck + 5 tests (pre-existing; complete fix
  exists but unmerged).
- **ai-learning-engine mission timed out** (15-min limit) for the third night
  running — needs a longer timeout or a smaller-scope run.

## Security or privacy concerns

- None new. No secrets touched. Research used public sources only. Renderer
  holds no network access; backend keys never reach renderer code.
- `NIGHT_LAB_ALLOW_CODE_CHANGES` / `NIGHT_LAB_ALLOW_RESEARCH` /
  `NIGHT_LAB_MAX_RISK` repo variables were **not readable** from this runner
  (API 403 — variables access not granted to the Actions token). All tonight's
  changes were treated as low-risk and none touch secrets; default `low` used.

## Required founder decisions

1. **Unblock night-lab PR creation** (persistent since 2026-07-24). Branches
   push fine now (3 pushed tonight), but `gh pr create` is expected to fail with
   "GitHub Actions is not permitted to create or approve pull requests" unless
   the repo-level "Allow GitHub Actions to create and approve pull requests"
   setting is enabled (or a PAT is provided). Verified again this run —
   see the PR attempt log on this summary's branch.
2. **Merge the web/ carrier branch**
   `nightly-backend-and-sync-web-contract-alignment` to unblock `web/`
   typecheck + 5 tests. Long overdue (since 2026-07-29). Re-verified clean
   tonight.
3. **Fix the ALE timeout.** The ai-learning-engine mission has timed out at the
   15-minute limit three nights running (08-02, 08-03, 08-05). Increase
   `timeout-minutes` for that job or shrink the ALE scope.
4. **Reconcile the stale backlog**: 18 `companion.tsx` branches (merge one per
   day, resolving the file), 10 stale ALE branches, several desktop-overlay
   branches. All unmerged, most with no PR.
5. **From tonight's research (metacognition):** decide whether to adopt the
   data-model rule that "I understand" is a UX affordance, not a verified
   comprehension signal (distinct `understood_self` vs `demonstrated_check`
   event types) — schema-additive, v1. Also decide v1 vs v2 for a
   generation-style "Test me" prompt and the calibration readout. See
   `docs/research/automated/2026-08-05-metacognition-self-assessment-calibration.md`.

## Recommended review order

1. `nightly-backend-and-sync-web-contract-alignment` (carrier — unblocks web/).
2. `nightly-desktop-overlay-bar-placement-toggle-off` (small, disjoint, tested).
3. `nightly-product-design-and-accessibility-quiz-radiogroup-keyboard` (app-only, tested).
4. `nightly-competitor-research-and-v2-metacognition-calibration` (docs only, quick read — contains the founder decisions above).

## Recommended merge order

1. web-contract-alignment first (unblocks the suite).
2. Then desktop-overlay bar-placement-toggle-off.
3. Then PDA quiz-radiogroup-keyboard.
4. Then the research report (docs-only).
5. Do **not** batch-merge the stale `companion.tsx` branches — merge one per
   day, resolving the file each time.

## Suggested next daytime task

Unblock night-lab PR creation (repo-level GitHub Actions permission), merge the
web/ carrier branch, and raise/fix the ALE 15-minute timeout. Then run a manual
macOS keyboard pass on the two app branches (roving radio + bar placement)
before merging them.
