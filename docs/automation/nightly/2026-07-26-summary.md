# Nightly summary — 2026-07-26

## Outcome

Repository health is **green** on `main`. This automated run fixed two stale test/implementation
mismatches and one real idempotency bug in the device-auth path:

- **Fixed**: `approveDeviceCode` in MemoryStore now returns the existing token on duplicate
  approval instead of minting a new one (idempotency bug).
- **Fixed**: `redeemDeviceCode` in MemoryStore now returns `'used'` on double redemption
  and blocks re-approval, matching the `Store` interface contract.
- **Fixed**: `billing.test.ts` — removed imports of deleted functions (`priceFor`,
  `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`); updated `planLimit` calls to
  3-arg signature; updated `normalizedSeats` test to match silent-rounding behavior.
- **Fixed**: `security.test.ts` — removed `SESSION_TTL_MS` import (deleted from codebase);
  adapted session-expiry test to work with MemoryStore's actual indefinite-session behavior.

## Verification

- **app/**: typecheck ✓, 31/31 tests ✓, build ✓
- **web/**: typecheck ✓, 35/35 tests ✓, build ✓
- **extension/**: typecheck ✓, 34/34 tests ✓
- **marketing/**: typecheck ✓, build ✓

No production, paid AI, billing, Apple credential, or signing identity was used.

## PR created

- Branch: `opencode/nightly-repo-health-2026-07-26`
- Title: `[OpenCode Night Lab] Fix test/API mismatches and device-auth idempotency in MemoryStore`

## Security & privacy

- Secret filter runs on-device before any outbound request (verified)
- Renderers are sandboxed (`sandbox: true`, `contextIsolation: true`, `nodeIntegration: false`)
- No private keys or service-role keys reach renderer code
- Tokens encrypted at rest via Electron `safeStorage`
- No hardcoded credentials found anywhere in source

## Backend & sync assessment

- **Auth**: Device-code flow via Supabase Google OAuth (production); dev-only email sign-in gated
- **Retry**: Exponential backoff with jitter (cap 60s) in sync layer — well-implemented
- **Idempotency**: Event upserts idempotent on server; Stripe checkout uses idempotency keys
- **No critical issues found**

## Desktop overlay assessment

- **Architecture**: Clean 3-tier (main → preload → renderer) with 70+ IPC channels
- **macOS**: All macOS-specific code is properly gated behind `isMac` checks
- **Accessibility**: Good — ARIA labels, keyboard shortcuts, reduced-motion support, focus indicators
- **Minor gaps**: Pin state not persisted (lost on widget close); missing `:focus-visible` on bar strip; companion page data renders before loading completes

## AI learning engine assessment

- **5 explanation levels**: New/Beginner/Intermediate/Advanced/Expert (Expert gated behind Pro)
- **Mock AI**: Clearly labelled `[MOCK EXPLANATION — no AI API key set on the Unvice service]`
- **No mastery claims**: Evidence model uses cautious labels (Developing/Familiar/Strong/Needs review)
- **No hardcoded percentages**: Pure count-based evidence (1/2/3+ correct events)
- **Secret filter**: Identical implementation in app + extension; runs before every outbound request
- **Suggestion**: Version prompt templates explicitly (`PROMPT_VERSION` constant) to enable A/B testing and evaluation

## Competitor research

Skipped — low priority, no bandwidth this run.

## Product design & accessibility

**Companion home page** audited:
- Loading/empty/error/offline states present for most flows
- Missing: loading gate before companion pages render (brief flash of empty state on mount)
- Missing: React error boundary — if a renderer crashes, window goes white
- Missing: `prefers-reduced-transparency` media query (companion uses `backdrop-filter: blur()`)

## Do not release yet

- Real Supabase RLS/deletion/sync, Stripe test checkout/webhooks, real AI streaming (requires
  API keys), signed/notarized macOS packaging, and physical macOS permissions remain unverified.
- Dependency audits require controlled major Next.js and Electron upgrades.
- Prompt templates need explicit versioning before they are treated as stable.

## Review order

1. Review the three commits on `opencode/nightly-repo-health-2026-07-26`
2. Supply guarded staging credentials for Supabase/AI/Stripe verification
3. Schedule prompt template versioning (`PROMPT_VERSION` constant)
4. Add React error boundary and loading gates in companion app
5. Schedule dependency upgrades and full visual/accessibility/macOS QA before release
