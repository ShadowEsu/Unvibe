# Nightly Summary — 2026-07-26

**Mission**: Integration Review & Morning Summary
**Current time**: 2026-07-26T19:16:39Z UTC
**Previous summaries**: [July 24](./2026-07-24-summary.md) (8 branches), [July 25](./2026-07-25-summary.md) (15 branches)

---

## Repository Health (current main)

| Package    | npm install | typecheck | tests     | build |
|------------|-------------|-----------|-----------|-------|
| **app/**   | ok          | clean     | 31/31 pass | ok   |
| **web/**   | ok          | 8 errors¹ | 31/36 pass | ok²  |
| **extension/** | ok      | clean     | 34/34 pass | —    |
| **server/** | ok         | clean     | —         | —    |
| **marketing/** | ok      | clean     | 17/17 pass | ok   |

¹ **8 typecheck errors** — all in test files: `billing.test.ts` (5: stale imports `priceFor`, `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`; 2 missing `seats` arg) and `security.test.ts` (3: missing `SESSION_TTL_MS`, `MemoryStore()` constructor signature).

² **web/ build now passes** — the 54 source-code TS errors reported in the July 24 summary have been resolved on main. Only test-file type errors remain.

**5/36 web tests fail**: 2 billing (stale imports, `normalizedSeats` no longer throws), 3 security (`approveDeviceCode` not idempotent, `MemoryStore` no longer supports time injection, no `SESSION_TTL_MS`).

---

## Night Lab Branches Inspected

The July 25 summary covered 15 branches. Since then, **6 new branches** were created:

| Branch | Type | Files | Date | Conflict Risk |
|--------|------|-------|------|---------------|
| `nightly-ai-learning-engine-parsequestion-integer-validation` | Code + tests | `comprehension.ts`, `index.ts`, `comprehension.test.ts`, `learning.test.ts` | Jul 26 | **Medium** — `comprehension.ts` conflicts if any branch touches it |
| `nightly-backend-and-sync-20260724` | Test fixes + docs | `billing.test.ts`, `security.test.ts`, summary | Jul 26 | **Medium** — identical test fixes as `repository-health` branch |
| `nightly-competitor-research-v2-desktop-overlay-patterns` | Research + decisions | 2 research docs | Jul 26 | None |
| `nightly-product-design-and-accessibility-sr-only` | CSS fix | `companion.css` (1 line) | Jul 26 | **Medium** — `companion.css` also touched by `plan-page-a11y` and `settings-escape` |
| `nightly-product-design-and-accessibility-settings-escape` | Code + CSS | `companion.tsx`, `companion.css` | Jul 25 | **High** — adds to companion.tsx conflict set (5→6 conflicting branches) |
| `nightly-repository-health-20260724` | Test fixes + docs | `billing.test.ts`, `security.test.ts`, summary | Jul 25 | **Medium** — identical test fixes as `backend-and-sync` branch |

---

## Overlap & Conflict Analysis

### New: Duplicate test-fix branches
`nightly-backend-and-sync-20260724` and `nightly-repository-health-20260724` make **identical** changes to `web/test/billing.test.ts` and `web/test/security.test.ts`. Neither has been merged. Pick one version and close the other.

### companion.tsx conflict set grows: 6 branches
The original 5 (from July 24) plus `nightly-product-design-and-accessibility-settings-escape` adds Escape-key handling in the Settings modal:

1. **accessibility-poll-fix** — macOS-only poll
2. **overview** — `'local'` → `'free'` plan label
3. **companion-a11y** — `aria-label`, `aria-current`, `aria-hidden`
4. **plan-page-a11y** — skeleton, `role="alert"`, `aria-live`, `aria-busy`
5. **sidebar-a11y** — `role="alert"`, `aria-live`, `aria-label`
6. **settings-escape** — Escape-key listener + `.sr-only` CSS

### companion.css: 3 branches
- `plan-page-a11y` (skeleton CSS)
- `settings-escape` (`.sr-only` utility)
- `sr-only` (identical `.sr-only` utility — **duplicate**)

### `comprehension.ts` — 1 new branch
- `ai-learning-engine` adds `Number.isInteger` guard. If merged, must verify no other branch touches this file.

### No new preload conflicts
The `event-cleanup` branch (preload listener fix) remains conflict-free as the sole branch touching `preload.ts`.

---

## New Research Completed

| Report | Location | Key Finding |
|--------|----------|-------------|
| Desktop AI Overlay Interaction Patterns | `docs/research/automated/2026-07-24-desktop-ai-overlay-patterns.md` | 8 patterns and 5 anti-patterns from Wispr Flow, Pieces, Superwhisper, Cody, Copilot, kapa.ai |
| AI Code Review & Explanation Tools | `docs/research/automated/2026-07-25-ai-code-review-explanation-tools.md` | No competitor does post-hoc comprehension scaffolding |
| Desktop Overlay Code Explanation Landscape | `docs/research/automated/2026-07-24-desktop-overlay-code-explanation.md` | Gap analysis of overlay-based explanation products |

---

## Problems Discovered

### Unchanged from previous summaries
1. **companion.tsx 6-way conflict** — now with an additional Escape-key handler
2. **web/ 8 type errors + 5 test failures** — test files not aligned with source refactoring
3. **29 npm vulnerabilities in app/** — pre-existing
4. **`fullProductBuildEnabled` removal duplicated** across 3 branches
5. **Duplicate test-fix branches** — 2 branches with identical billing/security test fixes

### New
6. **`nightly-product-design-and-accessibility-sr-only` duplicates** the `.sr-only` CSS already added by `settings-escape`. Neither is merged, but `settings-escape` has a broader scope (Escape key + CSS).
7. **`nightly-backend-and-sync-20260724`** and **`nightly-repository-health-20260724`** are duplicate branches with identical test-file changes. Only one should be merged.
8. **`forSync` added to `app/test/learning.test.ts`** — the `ai-learning-engine` branch imports `forSync` from `../src/core/learning`. This function must exist in the learning module. Verify the implementation exists before merging.

### Unverified behavior
- All macOS-specific features: menu-bar, accessibility permissions, Dock, login items, `hiddenInset`, selection capture — **unverified on Linux runner**
- Widget resize timer edge case (requires window lifecycle testing)
- `settings.ts` silent persist failures (requires disk-full scenario)

---

## Security & Privacy

- **No new vulnerabilities** in any of the 6 new branches
- `ai-learning-engine` branch follows privacy rules — `forSync` strips `code` and `explanation` before sync (verified in the test)
- No Supabase keys or secrets in renderer code (verified across all branches)
- Secret filtering remains in `review.ts` (blocks before any remote request)
- No credentials committed to tracked files

---

## Required Founder Decisions

1. **companion.tsx reconciliation**: Now 6 branches to merge. Determine the merge order or assign a manual reconciliation.
2. **Duplicate test-fix branches**: `repository-health-20260724` and `backend-and-sync-20260724` — pick one, close the other.
3. **Duplicate `.sr-only` CSS**: `settings-escape` and `sr-only` — `settings-escape` has broader scope, close `sr-only`.
4. **`forSync` implementation**: Confirm `forSync` exists in `app/src/core/learning` before merging the `ai-learning-engine` branch.
5. **`revokeToken` + `historyPage` in Store**: Still needed (from July 24 summary). The web/ build passes now, so these may have been fixed on main.
6. **Night Lab PR permissions**: Still blocked from creating PRs. Need `createPullRequest` scope on GITHUB_TOKEN.

---

## Recommended Review Order

1. **`nightly-competitor-research-v2-desktop-overlay-patterns`** — Research only, zero risk
2. **`nightly-competitor-research-code-review-tools`** — Research only, zero risk
3. **`nightly-desktop-overlay-event-cleanup`** — Low-risk single-file fix, no conflicts, typechecked + tested
4. **`nightly-ai-learning-engine-parsequestion-integer-validation`** — Low-risk integer validation + `forSync` test, no preload/renderer conflicts
5. **Duplicate test-fix branches** — Pick one, merge either `repository-health` or `backend-and-sync`, close the other
6. **`nightly-desktop-overlay-window-guard`** — Most complete main/windows refactor, has tests
7. Remaining companion.tsx branches — After reconciliation decision

## Recommended Merge Order

1. Research branches — merge immediately (docs only)
2. `event-cleanup` — merge immediately (low risk, tested, no conflicts)
3. `ai-learning-engine` — quick integer validation + test improvement
4. One test-fix branch — merge to fix 8 type errors + 5 test failures in web/
5. `window-guard` — main/windows changes
6. companion.tsx — reconciled single commit (all 6 branches combined)
7. Close `overview` (superseded by `window-guard`)
8. Close `sr-only` (superseded by `settings-escape`)
9. Close duplicate test-fix branch

---

## Suggested Next Daytime Task

**Merge the 3 safe branches**: `event-cleanup` (preload listener), `ai-learning-engine` (integer validation), and one of the duplicate test-fix branches. These are reviewed, tested, and conflict-free. Total effort: ~20 minutes.

---

## Night Lab Status Summary

- **21 total branches** across all night lab runs
- **6 new branches** since the July 25 summary
- **0 PRs open** — all blocked by GITHUB_TOKEN permissions
- **3 branches** ready for immediate merge (1 code fix, 2 research)
- **6 companion.tsx branches** awaiting reconciliation
- **2 duplicate test-fix branches** needed — pick one
- **web/ improved**: builds pass, down from 54→8 type errors, 17→5 failing tests
- **app/ healthy**: 31/31 tests, typecheck clean, build OK
- **extension/ healthy**: 34/34 tests, typecheck clean
- **marketing/ healthy**: 17/17 tests, typecheck clean, build OK
- All macOS behavior remains "unverified on Linux runner"
