# Nightly summary — 2026-07-28

## Mission
desktop-overlay — inspect Electron main/preload/renderer, test cross-platform logic, fix low-risk issues.

## Repository health
- **app/**: 31/31 tests passed, typecheck passes, build passes
- All deps installed successfully (412 packages)
- Dependencies show 29 npm audit warnings (28 high, 1 critical) — pre-existing, not introduced
- Recent commits: demo video link update, waitlist/auth fixes

## Code inspected
- `app/src/main/main.ts` (979 lines) — Electron main process, IPC handlers, window lifecycle
- `app/src/main/windows.ts` (317 lines) — window creation, bar positioning, widget resize, companion
- `app/src/main/backend.ts` (244 lines) — HTTP client, auth, billing, sync
- `app/src/main/backendUrl.ts` (15 lines) — backend URL resolution
- `app/src/main/store.ts` (273 lines) — local persistent store with encrypted tokens
- `app/src/main/settings.ts` (198 lines) — settings persistence with quiet-hours logic
- `app/src/main/review.ts` (467 lines) — review pipeline with secret filter, streaming, comprehension
- `app/src/main/selection.ts` (101 lines) — macOS selection capture via Accessibility API
- `app/src/main/sync.ts` (113 lines) — outbox sync with exponential retry
- `app/src/preload/preload.ts` (111 lines) — sandboxed bridge
- `app/src/renderer/widget/widget.tsx` (847 lines) — review panel with tabs, quiz, follow-up
- `app/src/renderer/bar/bar.tsx` (256 lines) — learning island with hover expansion
- `app/src/renderer/companion/companion.tsx` (1514 lines) — full companion app (settings, study, history, quiz)
- `app/src/renderer/shared/logo.tsx` — SVG brand mark
- `app/src/renderer/shared/richText.tsx` — markdown-like renderer with syntax highlight

## Problems discovered

### Fixed: Unreachable "Shortcut & Capture" settings tab
- **Location**: `app/src/renderer/companion/companion.tsx:1315-1320`
- **Issue**: The `Shortcut & Capture` tab (shortcut recorder + Accessibility permission display) had no navigation entry in the settings sidebar. Users could not customize their activation keyboard shortcut from the companion app.
- **Fix**: Added `'Shortcut & Capture'` between `'General'` and `'Island'` in the settings nav array with keyboard icon
- **Branch**: `opencode/nightly-desktop-overlay-settings-nav`
- **PR**: Could not auto-create due to GitHub Actions restrictions; branch is pushed

## Cross-platform notes (macOS-only behavior)
- `selection.ts` — uses `osascript` for selection capture → labelled "unverified on Linux runner"
- `main.ts:accessibilityGranted` — macOS-only `systemPreferences.isTrustedAccessibilityClient`
- `windows.ts` — `titleBarStyle: 'hiddenInset'` on companion → macOS-specific
- `main.ts:231-234` — accessibility check guards macOS path
- All macOS behavior correctly gated behind `process.platform === 'darwin'` checks

## Security observations
- Preload context isolation + sandbox + no nodeIntegration: ✅
- Secret filtering before any remote request: ✅
- Token encrypted at rest via Electron safeStorage: ✅
- Backend URL resolution with proper local-dev vs package logic: ✅
- No Supabase keys in renderer code: ✅

## Performance observations
- Widget resize uses `setInterval` at 16ms for live resize — reasonable
- Sync uses debounced outbox
- Learning store written synchronously on every event (JSON write) — could be batched for high-volume scenarios, but acceptable for current scale

## Required founder decisions
- None

## Recommended review order
1. Review the single-line fix on `opencode/nightly-desktop-overlay-settings-nav` (companion.tsx nav entry)
2. Verify shortcut recording works end-to-end on macOS (requires Accessibility permission)
3. Review npm audit warnings for pre-existing dependency vulnerabilities

## Recommended merge order
1. `opencode/nightly-desktop-overlay-settings-nav` — low risk, adds missing UI entry

## Suggested next daytime task
- Investigate whether the `Shortcut & Capture` tab content should be merged into the `General` settings tab instead of being a separate nav entry, for a cleaner settings layout.
