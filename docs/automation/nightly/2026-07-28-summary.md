# Nightly integration review — 2026-07-28

**Generated:** 2026-07-28T02:18:11Z  
**Mission:** integration-review-and-morning-summary  
**Reviewer:** OpenCode Night Lab

---

## Repository health

| Package | Typecheck | Tests | Build |
|---------|-----------|-------|-------|
| **app/** | ✅ pass | ✅ 31/31 pass | ✅ pass |
| **web/** | ❌ 8 errors (test files only) | ⚠️ 31/36 pass (5 pre-existing failures) | ✅ pass |
| **marketing/** | ✅ pass | ✅ 18/18 pass | ✅ pass |
| **extension/** | ❌ (parked — VS Code types unavailable) | ❌ 0/6 pass (expected) | ❌ MODULE_NOT_FOUND |

### Test failures (web/ — all pre-existing, not caused tonight)
- `test/billing.test.ts` — 2 failures: `priceFor`, `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent` not exported from `src/billing/plans` (API mismatch)
- `test/security.test.ts` — 3 failures: `SESSION_TTL_MS` not exported; function signature mismatch (expected 0 args, got 1); token IDs don't match expectations

### Known issues
- web/ test files are stale vs source API
- extension/ cannot be verified outside VS Code (parked)
- 29 npm audit warnings in app/ (pre-existing; 28 high, 1 critical)

---

## Branches created tonight (2026-07-28)

| Branch | Author | Changes | Files | Risk |
|--------|--------|---------|-------|------|
| `nightly-product-design-and-accessibility-onboarding-headings` | OpenCode Night Lab | h2→h1 heading levels in onboarding (4 lines) | `app/src/renderer/companion/companion.tsx` | Low |
| `nightly-desktop-overlay-settings-nav` | Unvibe Night Lab | Add "Shortcut & Capture" to settings nav (1 line) + premature summary | `app/src/renderer/companion/companion.tsx`, `app/package-lock.json`, `docs/automation/nightly/2026-07-28-summary.md` | Low |
| `nightly-ai-learning-engine-comprehension-validation` | OpenCode Night Lab | Validate non-empty fields in parseQuestion; 122-line test suite | `web/src/ai/comprehension.ts`, `web/test/comprehension.test.ts` | Low |
| `nightly-competitor-research-and-v2-code-explanation-landscape` | OpenCode Night Lab | 239-line research report on code explanation landscape | `docs/research/automated/2026-07-28-code-explanation-landscape.md` | None |

**Total: 4 branches. 0 open PRs.**

---

## Overlap and compatibility analysis

### File conflicts
1. **`app/src/renderer/companion/companion.tsx`** — modified by two branches:
   - `nightly-product-design-and-accessibility-onboarding-headings`: changes `Onboarding` component (lines 316, 326, 342, ~355)
   - `nightly-desktop-overlay-settings-nav`: changes `Settings` component (line ~1260)
   - **Verdict:** ~900 lines apart → auto-merge likely, no true conflict

### Premature summary file
- `nightly-desktop-overlay-settings-nav` wrote `docs/automation/nightly/2026-07-28-summary.md` from the desktop-overlay mission's perspective
- This should be owned by the integration-review mission (this file overwrites it)

### Semantic compatibility
- All changes are independent and orthogonal
- No incompatible API changes
- No shared data structures or stores affected

---

## Security and privacy observations

- **No Supabase keys in renderer code** ✅ (verified in app/src/renderer/)
- **Preload context isolation** ✅
- **Token encryption via Electron safeStorage** ✅
- **Secret filtering before remote request** ✅ (in app/src/main/review.ts)
- **Research report** uses only public sources ✅
- Research notes competition's `.codeiumignore`/`.cursorignore` patterns — Unvibe's `.unvibeignore` + proactive secret scan is a genuine differentiator

---

## Cross-platform / macOS notes

Unverified on Linux runner (all macOS behavior labelled):
- `selection.ts` — `osascript` for selection capture
- `main.ts:accessibilityGranted` — `systemPreferences.isTrustedAccessibilityClient`
- `windows.ts` — `titleBarStyle: 'hiddenInset'` on companion
- All gated behind `process.platform === 'darwin'`

---

## Required founder decisions

1. **Merge order:** branches are independent and can merge in any order. Recommend: onboarding-headings → settings-nav → comprehension-validation → research-report
2. **web/ test failures:** 5 pre-existing test failures need triage — are billing and security tests still relevant or should they be updated to match current API?
3. **extension/ future:** typecheck/build/test all fail in CI without VS Code. Consider a CI skip or conditional test strategy for the parked extension package
4. **npm audit (29 warnings, 1 critical):** pre-existing but worth scheduling maintenance

---

## Recommended review order

1. `nightly-product-design-and-accessibility-onboarding-headings` — 4 lines, immediately clear benefit (document hierarchy)
2. `nightly-desktop-overlay-settings-nav` — 1 logic line + package-lock update; restores missing UI navigation
3. `nightly-ai-learning-engine-comprehension-validation` — strengthens validation + comprehensive tests
4. `nightly-competitor-research-and-v2-code-explanation-landscape` — read-only report, useful strategic input

---

## Suggested next daytime task

Triage the 5 pre-existing web/ test failures. These are stale tests that no longer match the source APIs:
- `test/billing.test.ts` — exports `priceFor`, `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent` were removed or renamed
- `test/security.test.ts` — `SESSION_TTL_MS` export and function signatures changed
- Fixing these will restore web/ test suite to clean (36/36)
