# Nightly Summary — 2026-07-27

**Mission**: Integration Review & Morning Summary
**Current time**: 2026-07-27T01:52:39Z UTC
**Previous summary**: [July 26](./2026-07-16-summary.md)

---

## Repository Health (current main)

| Package    | npm install | typecheck | tests       | build |
|------------|-------------|-----------|-------------|-------|
| **app/**   | ok          | clean     | 31/31 pass  | ok    |
| **web/**   | ok          | **8 errors** | 31/36 pass **(5 fail)** | ok |
| **extension/** | ok      | clean     | 34/34 pass  | n/a   |
| **marketing/** | ok      | clean     | 18/18 pass  | ok    |
| **server/** | ok         | clean     | n/a         | n/a   |

**Web failures (unchanged from July 26):**
- Typecheck: 8 errors in `web/test/billing.test.ts` (5 stale imports) and `web/test/security.test.ts` (3 stale MemoryStore references)
- Tests: 5 failures — 2 billing (stale `priceFor` import, `normalizedSeats` no longer throws), 3 security (device-auth idempotency, expired codes, session expiry)

**No night lab branches have been merged to main.** All 21 remote branches remain unmerged.

---

## Night Lab Branches — Status

### 3 new branches created tonight (July 27)

| Branch | Change | Risk | Conflict |
|--------|--------|------|----------|
| `nightly-product-design-settings-focus-trap` | Focus trap in Settings modal + `aria-current` on sidebar nav (`companion.tsx`) | Low | **HIGH** — 7th branch touching companion.tsx |
| `nightly-ai-learning-engine-skill-decay` | Time-based skill decay: `decaySkillState` + `daysSince` in `learning.ts` + parallel decay in `web/src/data/skills.ts` | Low | **HIGH** — conflicts with `derive-skill-state-degrade` on `learning.ts` |
| `nightly-ai-learning-engine-derive-skill-state-degrade` | Degrade one level on slip with strong history (`learning.ts`) | Low | **HIGH** — conflicts with `skill-decay` on `learning.ts` |

### 18 unmerged branches from previous nights

| Branch | Date | Status |
|--------|------|--------|
| `nightly-integration-review-and-morning-summary-20260726` | Jul 26 | Docs only |
| `nightly-ai-learning-engine-parsequestion-integer-validation` | Jul 26 | Low risk, 4 files |
| `nightly-repo-health-2026-07-26` | Jul 26 | Fixes 8 typecheck errors + 5 test failures |
| `nightly-product-design-and-accessibility-sr-only` | Jul 26 | 1-line CSS (duplicate of settings-escape) |
| `nightly-competitor-research-v2-desktop-overlay-patterns` | Jul 26 | Research + founder decisions |
| `nightly-backend-and-sync-20260724` | Jul 26 | **Duplicate** of repo-health (test fixes) |
| `nightly-competitor-research-desktop-overlay` | Jul 25 | Research only |
| `nightly-competitor-research-code-review-tools` | Jul 25 | Research only |
| `nightly-product-design-and-accessibility-settings-escape` | Jul 25 | Escape handler + companion.tsx CSS |
| `nightly-desktop-overlay-event-cleanup` | Jul 25 | Preload listener cleanup |
| `nightly-backend-and-sync-audit-staging-docs` | Jul 24 | Staging docs |
| `nightly-desktop-overlay-accessibility-poll-fix` | Jul 24 | macOS poll fix |
| `nightly-desktop-overlay-overview` | Jul 24 | Overview |
| `nightly-desktop-overlay-window-guard` | Jul 24 | Window guard |
| `nightly-product-design-accessibility-companion-a11y` | Jul 24 | a11y fixes |
| `nightly-product-design-and-accessibility-plan-page-a11y` | Jul 23 | Plan page a11y |
| `nightly-product-design-and-accessibility-sidebar-a11y` | Jul 23 | Sidebar a11y |
| `nightly-repository-health-20260724` | Jul 25 | **Duplicate** of repo-health + backend-and-sync |

---

## Overlap & Conflict Analysis

### CRITICAL: `app/src/core/learning.ts` — 2 branches conflict
- `nightly-ai-learning-engine-skill-decay` rewrites `deriveSkillState` to accept `now` param, calls `decaySkillState` and `daysSince`
- `nightly-ai-learning-engine-derive-skill-state-degrade` changes `needs_review` handling to degrade one level on slip with strong history
- **These will produce a merge conflict.** They need to be reconciled into a single version.

### CRITICAL: `app/test/learning.test.ts` — 3 branches conflict
- All three AI learning engine branches modify this file (skill-decay adds decay tests, derive-skill-state-degrade adds slip tests, parseQuestion adds forSync test)

### HIGH: `app/src/renderer/companion/companion.tsx` — 7 branches
- The original 6-way conflict (from July 24) plus `settings-focus-trap`:
  - accessibility-poll-fix, overview, companion-a11y, plan-page-a11y, sidebar-a11y, settings-escape, settings-focus-trap
- `settings-focus-trap` adds a `useRef` + `useEffect` focus trap and `aria-current` on nav buttons
- These are additive and likely mergeable if done in the right order

### DUPLICATE: 3 branches fix the same web/ test files
- `nightly-repo-health-2026-07-26`, `nightly-repository-health-20260724`, `nightly-backend-and-sync-20260724`
- All fix `billing.test.ts` (stale imports) and `security.test.ts` (MemoryStore constructor, SESSION_TTL_MS)
- `repo-health-2026-07-26` is the most complete (also fixes MemoryStore device-auth idempotency)
- **Pick one, close the other two**

### DUPLICATE: `.sr-only` CSS
- `nightly-product-design-and-accessibility-settings-escape` already adds `.sr-only` utility
- `nightly-product-design-and-accessibility-sr-only` adds identical CSS — **close it**

### Research-only branches (safe, no conflicts)
- `nightly-competitor-research-desktop-overlay` — code explanation landscape
- `nightly-competitor-research-code-review-tools` — comprehension gap analysis
- `nightly-competitor-research-v2-desktop-overlay-patterns` — interaction patterns

---

## Research Completed (all nights)

| Report | Location | Key Finding |
|--------|----------|-------------|
| Desktop AI Overlay Interaction Patterns | `docs/research/automated/2026-07-24-desktop-ai-overlay-patterns.md` | 8 patterns and 5 anti-patterns from Wispr Flow, Pieces, Superwhisper, Cody, Copilot, kapa.ai |
| AI Code Review & Explanation Tools | `docs/research/automated/2026-07-25-ai-code-review-explanation-tools.md` | No competitor does post-hoc comprehension scaffolding |
| Desktop Overlay Code Explanation Landscape | `docs/research/automated/2026-07-24-desktop-overlay-code-explanation.md` | Gap analysis of overlay-based explanation products |

**Note:** Research reports exist only on remote branches, not on main.

---

## Problems Discovered

### Carried over from July 26 (all unresolved)
1. **companion.tsx now 7-way conflict** — focus trap added tonight
2. **web/ 8 typecheck errors + 5 test failures** — still unfixed on main
3. **29 npm vulnerabilities in app/** — pre-existing
4. **Duplicate test-fix branches** — 3 branches, need cleanup
5. **Duplicate `.sr-only` CSS** — 2 branches

### New in this run
6. **learning.ts 2-way conflict** — `skill-decay` vs `derive-skill-state-degrade` both change `deriveSkillState`
7. **learning.test.ts 3-way conflict** — all AI learning engine branches modify it
8. **21 branches total, 0 merged** — the backlog is growing; no night lab output has been merged

### Unverified behavior
- All macOS features: menu-bar, Dock, accessibility permissions, `hiddenInset` — **unverified on Linux runner**
- Widget resize timer edge cases
- Settings persistence failures (disk-full scenario)
- Real Supabase auth flow (no credentials available)
- Real AI streaming (no ANTHROPIC_API_KEY set)

---

## Recommended Review Order

1. **Research-only branches** (3) — zero risk, docs only, merge immediately
2. **`nightly-desktop-overlay-event-cleanup`** — low-risk single-file preload fix, tested
3. **`nightly-ai-learning-engine-parsequestion-integer-validation`** — low-risk integer validation + forSync test
4. **One test-fix branch**: `nightly-repo-health-2026-07-26` — fixes 8 typecheck errors + 5 test failures on web/
5. **`nightly-product-design-settings-focus-trap`** — focus trap + aria-current (low risk, small change)
6. AI learning engine branches **after reconciliation** — `skill-decay` and `derive-skill-state-degrade` must be merged together
7. Remaining companion.tsx branches (6) — after reconciliation decision
8. Duplicate branches to close

## Recommended Merge Order

1. Merge all 3 research branches (docs only, no code)
2. Merge `event-cleanup` (preload, low risk)
3. Merge `parsequestion-integer-validation` (AI engine, low risk)
4. Merge `repo-health-2026-07-26` (fixes web/ tests and typecheck)
5. Merge `settings-focus-trap` (companion.tsx, small additive change)
6. Reconcile and merge `skill-decay` + `derive-skill-state-degrade` together
7. Close duplicate branches: `sr-only`, `repository-health-20260724`, `backend-and-sync-20260724`
8. Remaining companion.tsx branches after manual reconciliation

---

## Security & Privacy

- **No new vulnerabilities** in any of the 3 new branches
- **None of the 21 branches** introduce security issues
- Secret filtering verified in extension + app (blocks before any remote request)
- No Supabase keys or secrets in renderer code
- `forSync` in learning engine branch correctly strips `code` and `explanation`
- No credentials committed to tracked files

---

## Required Founder Decisions

1. **learning.ts reconciliation**: `skill-decay` and `derive-skill-state-degrade` must be reconciled. The current branches produce a merge conflict. Decision needed on whether to include both time-based decay AND slip degradation.
2. **companion.tsx merge strategy**: 7 branches await reconciliation. A single manual merge combining all changes is the safest path.
3. **Duplicate branch cleanup**: Close `nightly-repository-health-20260724`, `nightly-backend-and-sync-20260724`, and `nightly-product-design-and-accessibility-sr-only`.
4. **Night Lab PR permissions**: All 21 branches are unmerged. If PR creation was blocked, unblock it. The backlog is growing unsustainably.
5. **21 unmerged branches**: With no merges happening, the night lab is producing output that isn't being integrated. Consider a review session or automated merge for low-risk branches.

---

## Night Lab Status Summary

- **21 total branches** across all night lab runs
- **3 new branches** since the July 26 summary
- **0 PRs open** — all blocked (no PRs exist for any branch)
- **21 branches unmerged** — none merged to main or any target branch
- **3 research branches** ready for immediate merge
- **1 low-risk code branch** ready (`event-cleanup`)
- **1 medium-risk code branch** ready (`parsequestion-integer-validation`)
- **1 critical web fix branch** ready (`repo-health-2026-07-26` — fixes 8 type errors, 5 test failures)
- **companion.tsx: 7 branches** awaiting reconciliation
- **learning.ts: 2 branches** conflicting, need reconciliation
- **3 duplicate branches** to close
- All macOS behavior remains "unverified on Linux runner"
