# Nightly summary — 2026-07-31

Integration-review mission. Reconciled the 4 branches and 6 reports produced by
tonight's run. All repository checks below were re-run fresh on `main` during
this review.

## Repository health (verified 2026-07-31, fresh on main)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **31/31 pass** | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | n/a |
| `web/` | ok | **FAIL — 8 errors** | **31/36 pass (5 fail)** | **pass** |
| `server/` | ok | **pass** | no test script | n/a |
| `marketing/` | ok | **pass** | **18/18 pass** | **pass** |

`web/` failures are pre-existing and identical to 2026-07-29/30 reports (billing
`priceFor`/`planLimit` signatures, device-code/session expiry in MemoryStore).
**A complete fix already exists** on the unmerged branch
`nightly-repository-health-web-fixes` (verified fresh: 0 typecheck errors,
38/38 tests, build compiles). It should merge first.

## Branches opened tonight (4) — all code changes verified clean

| Branch | Change | Independent verification |
|--------|--------|--------------------------|
| `nightly-desktop-overlay-accelerator-formatting` | Unified shortcut rendering across bar/widget/companion (`app/src/core/accelerator.ts`) | app: 36/36 pass, typecheck clean, build ok |
| `nightly-product-design-and-accessibility-reduced-motion-rotation` | Pause stat rotation under reduced motion + quiz ARIA (`aria-pressed`, `role="status"`) | app: 31/31 pass, typecheck clean, build ok |
| `nightly-ai-learning-engine-concept-slug-grouping` | `web/src/data/skills.ts` keys on machine-facing concept slug | skills: 5/5 pass; web build ok (full suite still 34/39 — same 5 pre-existing failures) |
| `nightly-competitor-research-and-v2-learning-retention` | Docs only: research report + founder decisions | n/a |

## Reports created tonight

- `docs/automation/nightly/2026-07-31-summary.md` — desktop-overlay mission report.
  **Filename collision:** this is the same path my integration review writes to.
  Recommend renaming the desktop-overlay report to
  `2026-07-31-desktop-overlay.md` when that branch is prepared for merge.
- `docs/automation/nightly/2026-07-31-ai-learning-engine.md`
- `docs/automation/nightly/2026-07-31-product-design-and-accessibility.md`
- `docs/automation/nightly/2026-07-31-founder-decisions.md`
- `docs/product/design-system-drift-decision.md`
- `docs/research/automated/2026-07-31-developer-education-learning-retention.md`

## Overlapping files / incompatible recommendations

- `app/src/renderer/bar/bar.tsx` + `widget.tsx` are touched by **both** the
  accelerator-formatting and the reduced-motion branches. Test-merged them:
  auto-merge succeeds cleanly, 36/36 tests pass, typecheck + build clean.
  Merge together; no ordering problem.
- `web/src/data/skills.ts` is touched by tonight's concept-slug branch and the
  stale `nightly-ai-learning-engine-skill-decay` branch. Test-merged: clean.
- Stale `nightly-ai-learning-engine-*` branches (5 total) touch
  `web/src/ai/comprehension.ts`, `app/src/core/learning.ts`,
  `web/src/data/skills.ts`. All merge cleanly into tonight's concept-slug
  branch — integrate them as one batch.
- No incompatible recommendations found. The research report (loop closure,
  level gating) is v1/v2 planning input, not conflicting code.

## Critical systemic issue — PR creation blocked (all runs)

Every mission tonight (and since 2026-07-24) hit the same blocker: the GitHub
Actions token **cannot create or approve PRs** (repo setting "Allow GitHub
Actions to create and approve pull requests" is disabled). Result:

- **None of the ~48 nightly branches have a PR.** `main` is at `af566ca`
  (2026-07-29) and contains **zero night-lab work** — including every earlier
  summary (07-23 → 07-30) and all fixes.
- Branches are pushed and ready. Manual PR URLs are listed in each branch's
  report.
- This integration-review branch hit the same blocker on its own PR
  (`createPullRequest` 403). Manual PR URL:
  https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-integration-review-and-morning-summary-20260731

## What was not verified

- macOS-specific behavior (window geometry, `selection.ts` osascript capture,
  Dock/menu-bar) — **unverified on Linux runner**, correctly labelled in each
  branch.
- Screen-reader announcement behavior not exercised on a real assistive device.
- No visual/dom test harness exists for renderer JSX; the two app branches add
  no renderer tests (their logic is in pure helpers + ARIA attributes).
- `NIGHT_LAB_ALLOW_CODE_CHANGES` / `NIGHT_LAB_ALLOW_RESEARCH` /
  `NIGHT_LAB_MAX_RISK` were **not readable** by the runner (403 on the
  variables API); conservative defaults applied (no code changes in research
  mission, max risk low).

## Security & privacy

- No credentials or API keys in any branch. Backend keys stay in the main
  process; renderers see only `{ userId, email }` (verified).
- Secret filter runs on-device before any network call (unchanged).
- Research uses public sources only; no proprietary material copied.

## Required founder decisions

1. **Unblock night-lab PRs.** Either enable "Allow GitHub Actions to create
   and approve pull requests" in repo settings, or supply a fine-grained PAT
   with `pull requests: write`. Without this, the night lab produces pushed
   branches that never get reviewed.
2. **Open tonight's 4 PRs manually** from the URLs in each branch report.
3. **Design-system drift** (`docs/product/design-system-drift-decision.md`):
   choose Option A (re-enforce strict monochrome) or Option B (adopt shipped
   purple/gradient system as the spec). Option B is recommended by the audit.
4. **Adopt v1 loop closure** from the research report? (wrong quiz answer →
   lesson at top of Study queue "Revisit today"; correct → existing 1/3/7/14d
   schedule). Mostly wiring; needs a yes/no.
5. **Decide merge-or-close** for the ~44 stale nightly branches, especially the
   5 ai-learning-engine branches that should be integrated together.

## Recommended review & merge order

1. `nightly-repository-health-web-fixes` — fixes main's 8 typecheck errors +
   5 test failures; unblocks CI. Verified 0 errors, 38/38 pass.
2. `nightly-desktop-overlay-accelerator-formatting` — small, tested (36/36).
3. `nightly-product-design-and-accessibility-reduced-motion-rotation` — small,
   tested (31/31). (2 and 3 touch the same files; merge in sequence or one PR.)
4. `nightly-ai-learning-engine-concept-slug-grouping` — additive web change;
   web CI is red until #1 lands.
5. Stale ai-learning-engine branches (5) — integrate as one batch with #4.
6. Research/docs branches — low risk, no code.

## Deferred (needs founder sign-off)

- Design-system drift resolution (decision #3 above).
- Level gating by demonstrated understanding (v2).
- Any dependency upgrades — none performed tonight.
- Model defaults (`claude-sonnet-5`, `gemini-3.1-flash-lite`) still reference
  fictional IDs (carried forward).

## Suggested next daytime task

Merge #1 (web CI fix) and open the 4 manual PRs; then review #2/#3. Fix the
PR-permission blocker so the night lab can open its own PRs tomorrow.
