# Repository health — 2026-07-26

## Status: GREEN

All packages install, typecheck, test, and build successfully.

## Packages

| Package | Typecheck | Test | Build |
|---------|-----------|------|-------|
| app/    | ✓         | 31/31 ✓ | ✓ |
| web/    | ✓         | 35/35 ✓ | ✓ |
| extension/ | ✓      | 34/34 ✓ | n/a |
| marketing/ | ✓      | n/a    | ✓ |

## Changes made

1. **`web/src/data/memoryStore.ts`**: Fixed `approveDeviceCode` idempotency (return existing
   token on duplicate) and `redeemDeviceCode` return value (return `'used'` on double redeem,
   block re-approval after use). Added `redeemed` flag to `PendingDevice`.

2. **`web/test/billing.test.ts`**: Removed imports of deleted functions (`priceFor`,
   `proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`); updated `planLimit` calls to
   3-arg signature; updated `normalizedSeats` test for silent-rounding behavior.

3. **`web/test/security.test.ts`**: Removed `SESSION_TTL_MS` import (deleted from codebase);
   adapted session-expiry test for MemoryStore's indefinite session behavior.

## Defects found & fixed

- **Device-auth idempotency**: `approveDeviceCode` minted a new token on duplicate approval
  instead of returning the existing one. Fixed with early return when `entry.token` exists.
- **Double redeem contract mismatch**: `redeemDeviceCode` never returned `'used'` despite the
  `Store` interface declaring it. Added `redeemed` tracking flag.
- **Stale test imports**: Two test files referenced APIs that were removed during the beta
  refactoring. Updated to match current source.

## Recent commits (git log -10)

```
e0e053d Restyle Google device approval for Unvibe
eddac36 Restore web auth and billing build compatibility
75b22a7 Document production Google OAuth redirect
58a8995 Make selected-code capture reliable on macOS
4fc34f1 Add unlimited full-product desktop build
c90e402 Refine Island morph and compact panel
f59eb2b Smooth Island expansion animation
14a17ad Merge remote-tracking branch 'origin/main' into codex/design-pixel-launch-experiment
1f9aacb Restore neutral app light mode and darken site
695345a Intensify gradient treatment across app and site
```
