# Nightly summary — 2026-08-02

Integration-review mission. Reconciled the 4 branches and reports produced by
tonight's run. All repository checks below were re-run fresh on `main` during
this review and independently re-verified on each branch.

## Repository health (verified 2026-08-02, fresh on main)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **31/31 pass** | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | n/a |
| `web/` | ok | **FAIL — 8 errors** | **31/36 pass (5 fail)** | **pass** |
| `marketing/` | ok | **pass** | **18/18 pass** | **pass** |

`web/` on `main` is red with **8 typecheck errors** (missing `priceFor`,
`proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`, `SESSION_TTL_MS`, wrong
`planLimit`/`MemoryStore` arity) and **5 failing tests** (billing pricing math,
Teams seat validation, duplicate device approval idempotency, expired
device-code, opaque-session expiry). This is the same pre-existing breakage
reported 2026-07-29 → 08-01.

**Tonight's `nightly-backend-and-sync-web-contract-alignment` branch fixes all
of it.** Verified fresh: web typecheck clean (0 errors), **37/37 tests pass**
(+1 new regression), `next build` succeeds. This should merge first to unblock
CI.

## Branches opened tonight (4) — all independently verified

| Branch | Change | Independent verification |
|--------|--------|--------------------------|
| `nightly-backend-and-sync-web-contract-alignment` | Restores `web` auth contract in `MemoryStore` (device-code TTL 10m, one-time redeem, session TTL 30d, injectable clock, idempotent approve) + pricing exports in `plans.ts`; fixes stale tests | web typecheck clean, **37/37 pass**, build ok (was 8 errors / 5 fail on main) |
| `nightly-ai-learning-engine-duplicate-option-rejection` | `parseQuestion` rejects duplicate/empty options (case/whitespace-insensitive) so ambiguous grading can't corrupt mastery evidence | **7 new `parseQuestion` tests pass**; remaining 5 failures are the pre-existing ones this branch's base carries |
| `nightly-product-design-and-accessibility-widget-reduced-motion` | Widget text reveal honors `prefers-reduced-motion` (full text instantly, skip rAF loop) via pure `revealInitialState` helper | app typecheck clean, **35/35 pass** (4 new reveal tests), build ok |
| `nightly-competitor-research-and-v2-privacy-trust-positioning` | Docs only: research report + founder decisions | n/a (docs only) |

## Reports created tonight

- `docs/automation/nightly/2026-08-02-backend-and-sync.md`
- `docs/automation/nightly/2026-08-02-product-design-and-accessibility.md`
- `docs/automation/nightly/2026-08-02-founder-decisions.md`
- `docs/research/automated/2026-08-02-privacy-trust-positioning.md`
- **Gap:** the ai-learning-engine branch has **no report file** — its commit
  message and test results are the only record. Recommend adding
  `docs/automation/nightly/2026-08-02-ai-learning-engine.md` before merge.

## Overlapping files / incompatible recommendations

- **No overlap between tonight's 4 branches** — disjoint file sets
  (`web/src/data/memoryStore.ts`+`billing/plans.ts` vs
  `web/src/ai/comprehension.ts` vs `app/src/renderer/widget/widget.tsx`).
- **Test-merged the two web code branches** (backend-and-sync + ai-learning-engine):
  auto-merge succeeds cleanly, **44/44 tests pass**, typecheck clean. Merge
  together, no ordering problem.
- **Conflict with stale branches (do not batch-merge):** tonight's ai branch
  (`comprehension.ts` duplicate-option rejection) **conflicts add/add** with the
  stale `nightly-ai-learning-engine-comprehension-validation`,
  `-parsequestion-answerindex-guard`, and `-parsequestion-integer-validation`
  branches (all touch `web/src/ai/comprehension.ts` + the test file). The two
  `parsequestion-*` branches are byte-identical duplicates of each other. These
  must be reconciled by hand — tonight's branch is the one with the most
  current tests; the `comprehension-validation` branch (trim/validate all
  fields) is complementary logic worth folding in manually. **Not an
  auto-merge batch.**

## Tests run (fresh, this review)

- `web` main: typecheck 8 errors; tests 31/36 fail 5. `next build` ok.
- `web` on backend-and-sync branch: typecheck clean; **37/37 pass**; build ok.
- `web` on test-merged (backend + ai) branch: typecheck clean; **44/44 pass**.
- `app` main: typecheck clean; **31/31 pass**; build ok.
- `app` on widget-reduced-motion branch: typecheck clean; **35/35 pass**; build ok.
- `marketing` main: typecheck clean; **18/18 pass**; build ok.
- `extension` main: typecheck clean; **34/34 pass**.

## Failures

- `web` on `main` still fails typecheck + 5 tests (pre-existing; fixed on the
  backend-and-sync branch).

## Unverified behavior

- **Supabase staging** (RLS, device-flow RPCs, remote sync/revocation/deletion)
  not executed — no credentials available. `MemoryStore` now mirrors the
  migration contract but `SupabaseStore` remains unverified.
- **macOS reduced-motion + widget reveal**: `matchMedia(prefers-reduced-motion)`
  is standard Chromium, but real-Mac behavior with the system toggle is
  **unverified on Linux runner**.
- Screen-reader announcement behavior not exercised on a real assistive device.
- No visual/DOM test harness exists for renderer JSX; widget change logic lives
  in a pure helper covered by node tests.
- `NIGHT_LAB_ALLOW_CODE_CHANGES` / `NIGHT_LAB_ALLOW_RESEARCH` /
  `NIGHT_LAB_MAX_RISK` still **not readable** by the runner (HTTP 403 on the
  variables API). Conservative defaults applied (max risk low, docs-only
  research).

## Security & privacy

- No credentials or API keys in any branch. Backend keys stay in the main
  process; renderers see only `{ userId, email }` (re-verified unchanged).
- Secret filter remains on-device before any network call (unchanged).
- Research uses public sources only; no proprietary material copied.
- Positive security change: device-code and session expiry now enforced in the
  dev store, matching production migrations.

## Required founder decisions

1. **Unblock night-lab PRs.** Same blocker as every run since 2026-07-24: the
   Actions token cannot create/approve PRs (repo setting disabled). Branch URLs
   are in each mission report. Enable the setting or supply a fine-grained PAT.
2. **Merge `nightly-backend-and-sync-web-contract-alignment` first** — it fixes
   the red web suite and unblocks CI. Verified clean above.
3. **Reconcile the stale `comprehension.ts` branches by hand** (5+
   ai-learning-engine branches): 2 byte-identical duplicates, 1 superset with
   complementary validation, and tonight's new duplicate-option rejection that
   conflicts add/add with all three. Merge deliberately, not as a batch.
4. **Research report decisions** (`docs/automation/nightly/2026-08-02-founder-decisions.md`):
   adopt the 3 v1 trust-copy items? extend preview-before-send to all requests
   (v1 or v2)? Hold the "no training" claim until provider verification?
5. **PR-blocker logging loop:** consider giving the workflow `actions: read`
   variable access so NIGHT_LAB_* limits can actually be honored.

## Recommended review & merge order

1. `nightly-backend-and-sync-web-contract-alignment` — unblocks CI (web 37/37,
   typecheck clean, build ok). Verified.
2. `nightly-ai-learning-engine-duplicate-option-rejection` — merge with #1
   (test-merged 44/44). Add the missing report file first.
3. `nightly-product-design-and-accessibility-widget-reduced-motion` — app
   35/35, build ok; independent of #1/#2.
4. `nightly-competitor-research-and-v2-privacy-trust-positioning` — docs only;
   read the founder decisions, then decide.

## Deferred / rejected

- Stale `comprehension.ts` branches (parsequestion×2, comprehension-validation,
  skill-decay, derive-skill-state-degrade): **not an auto-merge batch**; hand
  reconciliation needed (see decisions #3 and the 07-31 summary's
  deriveSkillState conflict).
- No broad dependency upgrades performed (policy).
- No `main` changes made by this review; all findings pushed to the
  `nightly-integration-review-and-morning-summary-20260802` branch.
