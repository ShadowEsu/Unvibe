# Nightly summary — 2026-08-07

Integration-review mission. Inspected every night-lab branch and report from
tonight (08-07) plus the two 08-06 branches that were never summarized (the
08-06 integration-review runs failed at 16:18/17:00/18:02Z), test-merged them
onto a fresh `main`, and re-ran repository checks. Nothing was merged.

## Repository health (verified fresh this review)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **31/31 pass** | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | pass (esbuild) |
| `marketing/` | ok | **pass** | **19/19 pass** | **pass** |
| `web/` | ok | **FAIL — 8 errors** | **31/36 pass (5 fail)** | **pass** |

`web/` on `main` is still red with the **same pre-existing breakage flagged
every night since 2026-07-29** (stale test imports of `priceFor`,
`proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`, `SESSION_TTL_MS`; wrong
`planLimit`/`MemoryStore(now)` arity) plus the **same 5 failing tests**. Tonight's
`session-ttl-parity` branch fixes all of it (see below).

## Tonight's runs (2026-08-07)

Workflow runs tonight: 11:08 ✓, 11:57 ✓, 13:40 ✗ (cancelled), 14:17 ✗
(cancelled), 15:10 ✓, 16:09 (this mission). Three of the cancelled/failed slots
mean **repository-health, desktop-overlay, ai-learning-engine and
competitor-research produced no branch tonight** (same schedule-delivery/15-min
timeout flakiness seen all week).

| Mission | Run | Result |
|---------|-----|--------|
| backend-and-sync | 11:08 | Stale web-test + dev-store contract fix — **pushed** |
| backend-and-sync | 11:57 | Session-TTL parity (on top of 11:08) — **pushed** |
| (2 slots) | 13:40, 14:17 | **cancelled** (no branch) |
| product-design-and-accessibility | 15:10 | Widget Depth radiogroup keyboard a11y — **pushed** |
| integration-review (this mission) | 16:09 | in progress |

## Branches opened tonight (3, all pushed) + pending 08-06 (2)

| Branch | Change | Verification (fresh, this review) |
|--------|--------|-------------------------------------|
| `nightly-backend-and-sync-stale-tests-20260807` | Restores device-code contract in dev `MemoryStore` (10-min TTL, single-use redemption, idempotent approval), fixes web test imports/signatures, hardens `normalizedSeats` | web typecheck clean; **34/34 pass**; build ok |
| `nightly-backend-and-sync-session-ttl-parity` (subsumes the above) | Enforces 30-day opaque-session expiry in `MemoryStore` + one read-side `SupabaseStore` query filter; injectable clock + regression test | web typecheck clean; **35/35 pass**; build ok |
| `nightly-product-design-and-accessibility-widget-depth-radiogroup` | `app/src/renderer/widget/widget.tsx` Depth picker → `role="radiogroup"` with roving tabindex, Arrow/Home/End, locked-skipping (`levelPicker.ts`, +5 tests) | app typecheck pass; **36/36 pass**; build ok |
| `nightly-desktop-overlay-bar-placement-and-widget-keys` (08-06, never summarized) | `barPlacement.ts` fixes "Follow active display" toggle-OFF; `widgetKeys.ts` adds CommandOrControl + form-field guard to widget shortcuts (+12 tests) | app typecheck pass; **43/43 pass**; build ok |
| `nightly-product-design-and-accessibility-study-level-radio` (08-06, never summarized) | `companion.tsx` Study restudy level → roving radiogroup (`rovingRadio.ts`; **byte-identical** to the 08-05 quiz-radiogroup branch) | app typecheck pass; **37/37 pass**; build ok |

**Combined test-merge of all 5 onto `main`: 1 trivial conflict** (widget.tsx
import line — both the 08-06 widgetKeys and 08-07 levelPicker branches add an
import at the same location; resolved in 1 line). Merged state re-checked
fresh: app typecheck pass, **54/54 pass** (31 main + 5 + 7 + 6 + 5), build ok.

## Overlapping files / incompatible recommendations

1. **`app/src/renderer/widget/widget.tsx` — today's two app branches pile up.**
   The 08-07 widget-depth-radiogroup branch and the 08-06
   bar-placement-and-widget-keys branch both edit this file. They are additive
   in disjoint regions (keydown handler vs Depth row JSX); the only conflict is
   a single import line. **Merge order matters**: reconcile widget.tsx once
   (e.g. merge 08-06 first, then 08-07). Do not batch-merge blindly.
2. **`rovingRadio.ts` duplicate-file risk is zero.** The 08-05
   quiz-radiogroup-keyboard and 08-06 study-level-radio branches each introduce
   the *identical* `app/src/core/rovingRadio.ts` — verified byte-identical, so
   whichever merges first the second resolves cleanly. `companion.tsx` edits are
   in disjoint components (Quiz bar vs Study level row); test-merged clean.
3. **Do NOT merge the stale `nightly-backend-and-sync-web-contract-alignment`
   (08-02) carrier.** It restores exports `main` deliberately removed in
   `eddac36` and **conflicts with tonight's session-TTL work on
   `web/src/data/memoryStore.ts` and `web/src/billing/plans.ts`**. Tonight's
   `session-ttl-parity` branch already makes `main` green and is the
   correct superset. (Keyset pagination on the 08-01 keyset-sync branch remains
   a separate deferred concern.)
4. **`barPlacement.ts`/`main.ts` have a stale twin.** The 08-05
   `nightly-desktop-overlay-bar-placement-toggle-off` branch is superseded by
   the 08-06 `bar-placement-and-widget-keys` branch (same fix, evolved API
   shape + widgetKeys). Merge only the 08-06 branch; retire the 08-05 one.
5. **web/ test files** are touched by both tonight's branches and the stale
   carrier — the carrier is the conflicting one; tonight's pair is an ancestor
   chain (`stale-tests` → `session-ttl-parity`) so merging the latter alone is
   sufficient.

## Risky code / macOS testing requirements

- **All app-side a11y/geometry changes (widget Depth radiogroup, Study
  roving-radio, widget keys, bar placement) are unit-tested but the live
  keyboard/focus/multi-display behavior is unverified on the Linux runner.**
  Each needs a short real-Mac pass before merge (VoiceOver announcement for the
  radios; ⌘W/⌘1–5 in the widget; follow-active-display toggle-off moving the
  strip). All four reports label this explicitly.
- **`SupabaseStore.userForToken` expiry filter is static-inspected +
  typechecked only** — no credentials on this runner. Must be exercised by the
  guarded `npm run verify:staging` release gate (see `docs/staging-test-plan.md`).
- **No AI work tonight** — no mock-AI labels needed. No research report was
  produced (competitor-research slot cancelled).

## Tests run (fresh, this review)

- `app` main: typecheck pass; **31/31 pass**; build ok.
- `extension` main: typecheck pass; **34/34 pass**; build ok.
- `marketing` main: typecheck pass; **19/19 pass**; build ok.
- `web` main: **8 typecheck errors + 5 test failures** (31/36) — pre-existing.
- `session-ttl-parity` branch: web typecheck clean; **35/35 pass**; build ok.
- `widget-depth-radiogroup` branch: app typecheck pass; **36/36 pass**; build ok.
- Combined test-merge (all 5 branches on fresh `main`): 1-line widget.tsx
  import conflict resolved; app typecheck pass; **54/54 pass**; build ok.

## Failures

- `web/` on `main` still fails typecheck + 5 tests (pre-existing; complete fix
  exists tonight on `session-ttl-parity`, unmerged).
- **08-06 integration-review never ran** — its 16:18/17:00/18:02Z slots all
  failed, leaving 08-06 with no summary; this summary carries those two branches
  forward.
- Two of tonight's seven slots were cancelled (13:40, 14:17) — recurring
  schedule-delivery flakiness.

## Security or privacy concerns

- None new. No secrets touched; no Supabase service-role key in any branch;
  renderer holds no network access. Session-expiry enforcement in both stores is
  a net positive. Research/competitor work: none tonight.
- Repo variables (`NIGHT_LAB_ALLOW_CODE_CHANGES`/`ALLOW_RESEARCH`/`MAX_RISK`)
  were **not readable** from this runner (API 403). All tonight's changes are
  additive, low-risk; default `low` assumed.

## Required founder decisions

1. **Unblock night-lab PR creation** (persistent since 2026-07-24): enable
   "Allow GitHub Actions to create and approve pull requests" for this repo.
   Branches push fine; `gh pr create` returns
   `createPullRequest: GitHub Actions is not permitted to create or approve
   pull requests`. 5 branches are currently unpushed-to-PR. Without this, every
   run must be merged by hand.
2. **Approve the session-TTL web fix for merge.** Recommend merging
   `session-ttl-parity` (it also makes `web/` green on `main`), with the
   SupabaseStore filter verified via `verify:staging` as a release gate. Decide:
   merge now with static evidence and gate staging, or hold for the staging run.
3. **macOS verification pass for the app branches** (see risk section) — decide
   approve-after-manual-pass vs merge-with-unit-tests-and-defer-the-Mac-pass for
   the three app branches.
4. **Retire the stale 08-05 `bar-placement-toggle-off` and 08-02
   `web-contract-alignment` branches** rather than merge them (superseded /
   conflicting respectively).

## Recommended review order

1. `nightly-backend-and-sync-session-ttl-parity` (unblocks web/; supersedes stale-tests + stale carrier).
2. `nightly-desktop-overlay-bar-placement-and-widget-keys` (08-06; app-only, tested).
3. `nightly-product-design-and-accessibility-widget-depth-radiogroup` (08-07; reconcile widget.tsx after #2).
4. `nightly-product-design-and-accessibility-study-level-radio` (08-06; companion.tsx, tested).
5. `nightly-backend-and-sync-stale-tests-20260807` — **do not merge**; fully subsumed by #1.

## Recommended merge order

1. session-ttl-parity first (carrier — makes web green, carries both web fixes).
2. desktop-overlay bar-placement-and-widget-keys (08-06).
3. widget-depth-radiogroup (08-07) — reconcile widget.tsx with #2 (one import line).
4. study-level-radio (08-06) — rovingRadio.ts resolves to the 08-05 quiz branch's file.
5. **Skip/close** the stale carrier (08-02) and 08-05 bar-placement-toggle-off.

## Suggested next daytime task

Unblock night-lab PR creation; merge session-ttl-parity; then a short macOS
keyboard/geometry pass on the three app branches before merging them. Consider
widening the ai-learning-engine timeout so that mission stops failing nightly.
