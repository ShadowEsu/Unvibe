# Nightly summary — 2026-08-03

Integration-review mission. Inspected the 3 branches and 3 reports produced by
tonight's run, test-merged them, re-ran repository checks fresh, and verified
the state of yesterday's open items.

## Repository health (verified fresh, this review)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **45/45 pass** (integrated) | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | n/a (parked) |
| `web/` | ok | **FAIL — 8 errors** | **31/36 pass (5 fail)** | **pass** |
| `marketing/` | ok | **pass** | **19/19 pass** | **pass** |

`web/` on `main` is red with **8 typecheck errors** (missing `priceFor`,
`proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`, `SESSION_TTL_MS`;
wrong `planLimit`/`MemoryStore` arity) and **5 failing tests** (billing pricing
math, Teams seat validation, duplicate device-approval idempotency, expired
device-code, opaque-session expiry). This is the **same pre-existing breakage
flagged every night since 2026-07-29** — it has never been merged.

`web/` on yesterday's `nightly-backend-and-sync-web-contract-alignment` branch
fixes all of it. Re-verified fresh this review: typecheck clean, **37/37 pass**,
`next build` ok. That branch is still **not merged and has no PR**.

## Branches opened tonight (3) — all independently verified

| Branch | Change | Verification (fresh) |
|--------|--------|----------------------|
| `nightly-ai-learning-engine-sse-spec-compliance` | SSE parser now frames events on the full WHATWG line-ending set (LF/CRLF/CR) and accepts `data:` with optional space; +3 tests | app typecheck clean, build ok, 34/34 pass |
| `nightly-product-design-and-accessibility-widget-tabs` | Widget tab bar: real close buttons, WAI-ARIA roving tabindex, Arrow/Home/End nav, focus restore; pure `nextTabIndex` helper; +6 tests | app typecheck clean, build ok, 37/37 pass |
| `nightly-product-design-and-accessibility-companion-focus-states` | Companion Settings dialog: focus-on-open, Tab trap, Escape dismiss, focus restore; pure `focusTrap.ts` helper; +5 tests | app typecheck clean, build ok, 36/36 pass |

All three were test-merged onto `main` together this review: auto-merge
succeeds, **45/45 tests pass** (31 base + 3 + 6 + 5), typecheck clean, build
ok. The three source changes are disjoint and compatible.

## Reports created tonight

- `docs/automation/nightly/2026-08-03-ai-learning-engine.md`
- `docs/automation/nightly/2026-08-03-product-design-and-accessibility.md` —
  **same path on both product-design branches** (see below).

No research reports tonight (competitor-research mission did not run).

## Overlapping files / incompatible recommendations

1. **Shared report file (merge conflict).** Both product-design branches write
   `docs/automation/nightly/2026-08-03-product-design-and-accessibility.md`;
   their working trees carry different content → add/add conflict on merge.
   Source files are disjoint (`widget.tsx` vs `companion.tsx`), so this is the
   **only** conflict. Resolution: take the `companion-focus-states` version —
   it is a superset documenting both runs. Recommend merging widget-tabs first,
   then resolving the report file when merging companion-focus.
2. **Identical lockfile edits (no conflict).** All 3 branches bump
   `app/package-lock.json` 0.1.1 → 0.1.2 (matches `package.json`); identical
   change, merges cleanly.
3. **web/ fix overlap (pre-existing, still unresolved).** Six+ stale unmerged
   nightly branches all patch the same three files to fix the same web/ test
   failures: `web/test/billing.test.ts`, `web/test/security.test.ts`,
   `web/src/data/memoryStore.ts` (also `web/src/billing/plans.ts`).
   They will conflict with each other and with `main`'s churn. They are:
   `nightly-backend-and-sync-web-contract-alignment` (verified complete),
   `nightly-backend-and-sync-keyset-sync`,
   `nightly-auto-repository-health-20260731`,
   `nightly-repository-health-web-fixes`,
   `nightly-auto-test-fixes-and-audit`,
   `nightly-repo-health-2026-07-26`. **Do not batch-merge.** Use the
   web-contract-alignment branch as the single carrier (most current, fully
   verified) and manually fold in any unique changes from the others.

## Risky code / macOS testing requirements

- **Widget tab keyboard nav** (roving tabindex, focus restore after close) —
  logic is unit-covered but real-keyboard interaction in a live window is
  **unverified on Linux runner**. Requires a real-Mac pass.
- **Settings dialog focus trap** (focus on open, Tab cycle, Escape, restore) —
  same caveat: **unverified on Linux runner**; no driver/E2E harness exists in
  `app/` for renderer focus behavior.
- **SSE parser** — pure and platform-independent; fully unit-covered. Low risk.
- **web-contract-alignment** restores security semantics (10-min device-code
  TTL, one-time redemption, 30-day opaque-session expiry, idempotent approve)
  in `MemoryStore`, mirroring `SupabaseStore`/migrations — a positive change,
  but `SupabaseStore` itself remains **unverified** (no credentials).

## Tests run (fresh, this review)

- `app` integrated (main + all 3 branches): typecheck clean; **45/45 pass**; build ok.
- `app` per branch: 34/34, 37/37, 36/36 pass respectively; typecheck clean; build ok.
- `web` main: typecheck 8 errors; **31/36 pass (5 fail)**; `next build` ok.
- `web` on web-contract-alignment: typecheck clean; **37/37 pass**; build ok.
- `marketing` main: typecheck clean; **19/19 pass**; build ok.
- `extension` main: typecheck clean; **34/34 pass**.

## Failures

- `web/` on `main` still fails typecheck + 5 tests (pre-existing since before
  2026-07-29; complete fix exists but unmerged).

## Unverified behavior

- Supabase staging (RLS, device-flow RPCs, session expiry, account deletion) —
  no credentials available.
- macOS behavior of all three UI/parser changes — **unverified on Linux runner**.
- Screen-reader (VoiceOver/NVDA) pass not run.
- No visual/DOM test harness for renderer JSX; widget/focus logic is in pure
  helpers covered by node tests.

## Security & privacy

- No credentials or API keys in any branch; no `.env` committed.
- Backend keys stay in the Electron main process; renderers receive only
  `{ userId, email }` (unchanged, re-checked).
- Secret filter remains on-device before any network call (unchanged).
- No research this run; no external data involved.

## Required founder decisions

1. **Unblock night-lab PRs (blocker since 2026-07-24).** The Actions token
   cannot create/approve PRs ("GitHub Actions is not permitted to create or
   approve pull requests"). All three branches are pushed and ready; open PRs
   manually:
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-ai-learning-engine-sse-spec-compliance
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-product-design-and-accessibility-widget-tabs
   - https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-product-design-and-accessibility-companion-focus-states
   - also (yesterday): https://github.com/ShadowEsu/Unvibe/pull/new/opencode/nightly-backend-and-sync-web-contract-alignment
2. **Merge `nightly-backend-and-sync-web-contract-alignment` first** — it fixes
   the red web/ suite and unblocks CI. It has sat unmerged for a full day.
3. **Resolve the shared product-design report file** (take the
   companion-focus version) when merging the two product-design branches.
4. **Reconcile the 6 overlapping web/ fix branches** by hand, keeping
   web-contract-alignment as the carrier; do not batch-merge.
5. **Real-Mac QA pass** before merging the two UI/accessibility branches.

## Recommended review order

1. `nightly-backend-and-sync-web-contract-alignment` (unblocks web CI).
2. `nightly-ai-learning-engine-sse-spec-compliance` (small, safe, unit-covered).
3. `nightly-product-design-and-accessibility-widget-tabs`.
4. `nightly-product-design-and-accessibility-companion-focus-states`.

## Recommended merge order

1. web-contract-alignment → main.
2. SSE-spec-compliance → main (independent, no conflicts).
3. widget-tabs → main.
4. companion-focus → main (resolve report file to the superset version).

## Deferred / rejected

- The 5 other stale web/ fix branches — superseded by web-contract-alignment.
- The stale `web/src/ai/comprehension.ts` branches (parseQuestion ×2,
  comprehension-validation, skill-decay, deriveSkillState) — untouched by
  tonight; still need the hand-reconciliation flagged on 2026-08-02.
- No product-design redesign or dependency upgrades were attempted.

## Suggested next daytime task

Open the four PRs above (manual, token is blocked), review and merge
web-contract-alignment to turn web/ green, then run a short manual keyboard +
focus pass for the widget tabs and Settings dialog on a real Mac before
merging the two accessibility branches.
