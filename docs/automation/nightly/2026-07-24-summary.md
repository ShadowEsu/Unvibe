# Nightly Summary — 2026-07-24

**Mission**: Integration Review & Morning Summary
**Current time**: 2026-07-24T06:48:52Z UTC
**Repo variables**: NIGHT_LAB_ALLOW_CODE_CHANGES=unset, NIGHT_LAB_ALLOW_RESEARCH=unset, NIGHT_LAB_MAX_RISK=unset (defaults: disallowed, disallowed, low)

---

## Repository Health

| Package | npm install | typecheck | tests | build |
|---------|-------------|-----------|-------|-------|
| **app/** | ok | clean (0 errors) | 31/31 pass | build ok |
| **web/** | ok | **54 errors** | **19/36 pass (17 fail)** | **Failed to compile** |
| **extension/** | not tested | — | — | — |
| **marketing/** | not tested | — | — | — |

**web/ build failure**: Next.js compilation fails at `signout/route.ts:16` (`revokeToken` missing). 54 TS errors confirmed (identical to backend audit report).

---

## Night Lab Branches Inspected

Eight branches were found (all pushed to origin, no open PRs):

| Branch | Type | Files changed | Status |
|--------|------|---------------|--------|
| `nightly-backend-and-sync-audit-staging-docs` | Docs only | 3 files | Needs review |
| `nightly-competitor-research-desktop-overlay-comprehension` | Research only | 1 file | Needs review |
| `nightly-desktop-overlay-accessibility-poll-fix` | Code fix | 1 file (companion.tsx) | Needs review |
| `nightly-desktop-overlay-overview` | Code + docs | 12 files | Needs review |
| `nightly-desktop-overlay-window-guard` | Code + tests + docs | 14 files | Needs review |
| `nightly-product-design-accessibility-companion-a11y` | Code fix | 2 files | Needs review |
| `nightly-product-design-and-accessibility-plan-page-a11y` | Code fix + CSS | 12 files | Needs review |
| `nightly-product-design-and-accessibility-sidebar-a11y` | Code fix | 1 file | Needs review |

**None have open PRs** — GitHub Actions token lacks `createPullRequest` scope.

---

## Overlapping Files (CRITICAL)

### `app/src/renderer/companion/companion.tsx` — 5 conflicting change sets

1. **accessibility-poll-fix**: Only poll accessibility grant on macOS
2. **overview**: Change `'local'` → `'free'` plan label fallback
3. **companion-a11y**: Add `aria-label`, `aria-current`, `aria-hidden`
4. **plan-page-a11y**: Add skeleton loading, `role="alert"`, `role="radiogroup"`, `aria-live`, `aria-busy`
5. **sidebar-a11y**: Add `role="alert"` on errors, `aria-live` on sync, `aria-label` on sidebar

**These are mutually incompatible** — all modify the same JSX lines. Must be manually reconciled.

### `app/src/main/main.ts`, `trial.ts`, `usage.ts`, `selection.ts`, `contextBuilder.ts`, `build.mjs` — 3 branches

- **overview**, **window-guard**, **plan-page-a11y** all contain identical `fullProductBuildEnabled` removal
- **window-guard** adds platform guard on `setHiddenInMissionControl`
- **plan-page-a11y** has the fullProduct removal but differs in `windows.ts` (no geometry extraction)

These should be merged as one combined change. **window-guard** is the most complete version.

### `app/src/main/windows.ts` — 2 branches

- **window-guard**: Full refactor + geometry extraction + platform guards + tests
- **plan-page-a11y**: Only the `BAR_EXPANDED_H` constant change (no geometry extraction)

---

## Problems Discovered

### Critical
1. **web/ build broken** — `revokeToken` and `historyPage` missing from Store interface. Sign-out throws at runtime. Prevents any backend deployment.
2. **54 type errors in web/** — Interface/type misalignment (IncomingEvent, ProfileSummary, EventRecord, Store, plans.ts). All are additive type changes.
3. **17/36 web tests fail** — Primarily missing exports from `plans.ts` (11 billing tests) and missing Store methods (4 security + 1 pagination + 1 progress).

### Concerning
4. **companion.tsx has 5 conflicting change sets** — Must be manually merged. High risk of regression if merged naively.
5. **Three branches make identical `fullProductBuildEnabled` removal** — Duplicate effort, potential merge conflicts.
6. **No code changes have been built or typechecked together** — Each branch was tested in isolation. Combined state is unverified.
7. **macOS behavior unverified** — All `app/` changes (accessibility polling, window guards, selection) labelled as "unverified on Linux runner".

---

## Research Completed

- `docs/research/automated/2026-07-24-desktop-overlay-code-comprehension.md` — Competitor landscape analysis (Wispr Flow, Raycast, Pieces). Found that no competitor addresses code comprehension as a primary use case. Unvibe's position ("output understanding") is genuinely unique.

---

## Reports Created

| Report | Branch | 
|--------|--------|
| `docs/automation/nightly/2026-07-24-backend-and-sync-audit.md` | backend-and-sync-audit-staging-docs |
| `docs/automation/nightly/backend-sync-staging-verification.md` | backend-and-sync-audit-staging-docs |
| `docs/automation/nightly/2026-07-24-summary.md` (this run) | backend-and-sync-audit-staging-docs |
| `docs/automation/nightly/2026-07-23-desktop-overlay.md` | desktop-overlay-overview |
| `docs/automation/nightly/2026-07-23-summary.md` | desktop-overlay-window-guard |
| `docs/research/automated/2026-07-24-desktop-overlay-code-comprehension.md` | competitor-research-* |

---

## Security & Privacy

- **No vulnerabilities found** in any branch. Secret filtering, token encryption, and I/O architecture remain sound.
- `web/` has no secret/key leaks — the `revokeToken` and `historyPage` gaps are functionality, not security.
- All code changes are in `app/` (desktop), which enforces secret filtering before any network call.

---

## Required Founder Decisions

1. **`revokeToken` + `historyPage`**: Add to Store interface + MemoryStore + SupabaseStore? (unblocks sign-out, pagination). Estimated: 20-30 minutes.
2. **Companion.tsx reconciliation**: Which of the 5 companion.tsx change sets to keep, and in what order? Manual merge required.
3. **`fullProductBuildEnabled` removal**: Confirm the `'full'` plan is no longer needed (was used for founder builds). Three branches remove it — accept the **window-guard** version.
4. **web/ type alignment**: Backfill `localDate`/`timezone` into `IncomingEvent`, `conceptsFamiliar`/`conceptsStrong` into `ProfileSummary`, `language` into `EventRecord`?
5. **Billing plans.ts exports**: Were the 11 missing exports (`canManageBilling`, `effectivePlan`, `planLimit`, etc.) intentionally pruned or a regression?
6. **Night Lab permissions**: GitHub Actions token lacks `createPullRequest` scope — requested by all 8 branches. Enable for future runs.

---

## Recommended Review Order

1. **backend-and-sync-audit-staging-docs** (docs only, zero risk) — read the audit report first to understand web/ state
2. **competitor-research-desktop-overlay-comprehension** (docs only, zero risk)
3. **desktop-overlay-window-guard** (most complete code change, has tests, platform-guarded)
4. **desktop-overlay-overview** (small subset of window-guard; may be superseded)
5. **desktop-overlay-accessibility-poll-fix** (narrow fix, needs companion.tsx reconciliation)
6. **product-design-and-accessibility-sidebar-a11y** (companion.tsx aria changes)
7. **product-design-and-accessibility-companion-a11y** (companion.tsx aria changes)
8. **product-design-and-accessibility-plan-page-a11y** (companion.tsx skeleton + aria + fullProduct removal)

---

## Recommended Merge Order

1. **backend-and-sync-audit-staging-docs** — merge immediately (docs only)
2. **competitor-research-desktop-overlay-comprehension** — merge immediately (docs only)
3. web/ type alignment + missing Store methods — fix on main, then merge
4. **window-guard** → then cherry-pick companion.tsx from **accessibility-poll-fix** + **sidebar-a11y** + **companion-a11y** + **plan-page-a11y** (single reconciled commit)
5. **overview** — superseded by window-guard, close without merging
6. **plan-page-a11y** — superseded by combined companion.tsx merge

---

## Suggested Next Daytime Task

**Fix web/ build**: Add `revokeToken` and `historyPage` to Store interface, MemoryStore, and SupabaseStore. This unblocks the entire backend deployment and fixes 2 TS errors + 5 failing tests. **Estimated effort: 20-30 minutes.**

---

## Night Lab Status Summary

- 8 branches created, 0 PRs open
- 2 docs-only branches ready for immediate merge
- 1 critical build failure in web/
- 1 file (companion.tsx) with 5 conflicting change sets
- All code changes require macOS verification
- `fullProductBuildEnabled` removal duplicated across 3 branches
