# Nightly summary — 2026-08-04

Integration-review mission. Inspected tonight's 4 runs, test-merged the 3 pushed
branches, re-ran repository checks fresh, and reconciled against the stale
unmerged backlog. Nothing was merged.

## Repository health (verified fresh this review)

| Package | Install | Typecheck | Test | Build |
|---------|---------|-----------|------|-------|
| `app/` | ok | **pass** | **34/34 pass** | **pass** |
| `extension/` | ok | **pass** | **34/34 pass** | n/a (parked) |
| `web/` | ok | **FAIL — 8 errors** | **37/42 pass (5 fail)** | **pass** |
| `marketing/` | ok | **pass** | **19/19 pass** | **pass** |

`web/` on `main` is still red with the **same pre-existing breakage flagged
every night since 2026-07-29** (missing `priceFor`,
`proAnnualSavingsPercent`, `teamsAnnualSavingsPercent`, `SESSION_TTL_MS`;
wrong `planLimit`/`MemoryStore` arity) and **5 failing tests** (billing pricing
math, Teams seat validation, duplicate device-approval idempotency, expired
device-code, opaque-session expiry). The complete fix exists on the unmerged
`nightly-backend-and-sync-web-contract-alignment` branch (verified clean on
2026-08-03; still not merged, still no PR).

## Tonight's runs (2026-08-04)

Five of the seven scheduled missions ran. **repository-health (10:17) and
backend-and-sync (11:17) did not run tonight.**

| Mission | Run | Result |
|---------|-----|--------|
| desktop-overlay | 12:19 | Fix `barPlacement` toggle-off bug — **branch LOST** (push blocked) |
| ai-learning-engine | 13:09 | Fix comprehension prompt contradiction — **pushed** |
| competitor-research-and-v2 | 14:40 | Research report (quiet-review attention) — **pushed** |
| product-design-and-accessibility | 15:33 | History filter roving-tab a11y — **pushed** |
| integration-review (this mission) | 16:23 | in progress |

## Branches opened tonight (3 pushed, 1 lost)

| Branch | Change | Verification (fresh, test-merged on main) |
|--------|--------|--------------------------------------------|
| `nightly-ai-learning-engine-comprehension-task-fix` | `buildComprehensionPrompt` no longer emits the scope-derived explanation task into the user message; `buildUserPrompt` gains optional `task` override; +6 tests | web typecheck: only the 8 pre-existing errors; web tests: 37/42 (the 5 failures are the same pre-existing billing/security ones); build ok |
| `nightly-competitor-research-quiet-review-attention` | Research report only (docs/research/automated/2026-08-04-quiet-review-attention-design.md) | docs-only; no code impact |
| `nightly-product-design-and-accessibility-history-filter-tabs` | History filter becomes a real WAI-ARIA tablist: roving tabindex, Arrow/Home/End nav, tabpanel linkage; pure `nextRovingIndex` helper; +3 tests | app typecheck pass; **34/34 pass**; build ok |
| `nightly-desktop-overlay-bar-placement` | **Never pushed.** `barPlacementAction` pure helper + `main.ts` wiring so "Follow active display" can be toggled OFF | Verified in-run only (37/37 in runner); **the fix exists only in the runner log — it must be re-applied manually** |

## Overlapping files / incompatible recommendations

1. **Tonight's 3 pushed branches are disjoint.** ALE touches `web/src/ai/prompt.ts`
   (new territory — no stale branch touches it). PDA touches
   `app/src/renderer/companion/companion.tsx` + new `app/src/core/rovingTabs.ts`.
   Research is docs-only. Test-merge of all three onto `main`: **0 conflicts**.
2. **`companion.tsx` pile-up (biggest problem).** 16+ stale unmerged branches all
   edit `app/src/renderer/companion/companion.tsx` (all the product-design a11y
   branches + several desktop-overlay ones). Tonight's PDA branch is the 17th
   edit to that file. Do **not** batch-merge these; they will conflict with each
   other. Reconcile in order.
3. **web/ prompt.ts vs web/ comprehension.ts — safe.** Tonight's ALE branch
   edits `prompt.ts`; the stale comprehension branches
   (`duplicate-option-rejection`, `comprehension-validation`) edit
   `web/src/ai/comprehension.ts`. Different files, no overlap.
4. **Lost desktop-overlay fix does not overlap anything pushed tonight**, but its
   `main.ts` edit will conflict with the stale `nightly-desktop-overlay-window-guard`
   branch when both are eventually merged.

## Risky code / macOS testing requirements

- **PDA roving-tab keyboard nav** (Arrow/Home/End + focus movement in a live
  window) — logic unit-covered; real-keyboard behavior is **unverified on Linux
  runner**. Requires a real-Mac pass.
- **desktop-overlay bar placement** — the whole fix is **unverified on Linux
  runner** (macOS geometry) and is currently lost (never pushed).
- **Comprehension prompt change** — prompt-contract tests only; real-model
  output is **mock AI — real key required for verification**.
- **web-contract-alignment** (unmerged) restores security semantics (10-min
  device-code TTL, one-time redemption, 30-day opaque-session expiry) in
  `MemoryStore`; `SupabaseStore` remains unverified (no credentials).

## Tests run (fresh, this review)

- `app` (main + all 3 pushed branches): typecheck pass; **34/34 pass**; build ok.
- `web` (main + ALE branch): typecheck 8 errors (pre-existing); **37/42 pass**
  (5 pre-existing failures); `next build` ok.
- `marketing` main: typecheck pass; **19/19 pass**; build ok.
- `extension` main: typecheck pass; **34/34 pass**.

## Failures

- `web/` on `main` still fails typecheck + 5 tests (pre-existing, complete fix
  exists but unmerged).

## Security or privacy concerns

- None new. No secrets touched. Renderer holds no network access; backend keys
  never reach renderer code. All changes are on-device or backend-prompt text.

## Required founder decisions

1. **Unblock night-lab PR creation** (persistent since 2026-07-24). The runner
   token is read-only: `gh pr create` → "GitHub Actions is not permitted to
   create or approve pull requests"; `git push` → auth failed. The workflow
   already declares `contents: write`/`pull-requests: write`, but the repo-level
   permission "Allow GitHub Actions to create and approve pull requests" is
   disabled (or a PAT is needed). Until this is fixed, every tested fix must be
   pushed/PR'd by hand.
2. **Re-apply the lost desktop-overlay fix.** The `barPlacement` fix (toggle
   "Follow active display" off) is tested and ready but never left the runner.
   Re-create `app/src/core/barPlacement.ts` + the `main.ts` wiring from the
   2026-08-04 run log, or re-run the desktop-overlay mission with a working token.
3. **Merge the web/ carrier branch** `nightly-backend-and-sync-web-contract-alignment`
   to unblock `web/` typecheck + 5 tests. Long overdue (since 2026-07-29).
4. **Reconcile the stale backlog**: 16+ `companion.tsx` branches, 9 stale ALE
   branches, several desktop-overlay branches. All are unmerged, most have no PR.

## Recommended review order

1. `nightly-backend-and-sync-web-contract-alignment` (carrier — unblocks web/).
2. `nightly-ai-learning-engine-comprehension-task-fix` (small, disjoint, tested).
3. `nightly-product-design-and-accessibility-history-filter-tabs` (app-only, tested).
4. `nightly-competitor-research-quiet-review-attention` (docs only, quick read).
5. Re-apply `barPlacement` fix from run log (or re-run the mission).

## Recommended merge order

1. web-contract-alignment first (unblocks the suite).
2. Then ALE comprehension-task-fix (does not overlap the carrier).
3. Then PDA history-filter-tabs (app-only).
4. Then research report (docs-only).
5. Do **not** batch-merge the stale `companion.tsx` branches — merge one per day,
   resolving the file each time. The lost desktop-overlay fix should land via a
   fresh run after PR creation is unblocked.

## Suggested next daytime task

Fix the night-lab infrastructure: enable "Allow GitHub Actions to create and
approve pull requests" at the repo level (or provide a PAT) so the loop closes,
then re-run the desktop-overlay mission to recover the lost `barPlacement` fix.
