# Nightly Summary — 2026-07-25

**Mission**: Integration Review & Morning Summary
**Current time**: 2026-07-25T16:13:00Z UTC
**Repo variables**: NIGHT_LAB_ALLOW_CODE_CHANGES=unset, NIGHT_LAB_ALLOW_RESEARCH=unset, NIGHT_LAB_MAX_RISK=unset (defaults: disallowed, disallowed, low)
**Previous review**: 2026-07-24 summary reviewed 8 branches — [see full report](../2026-07-24-summary.md)

---

## Repository Health (app/)

| Check | Result |
|-------|--------|
| `npm install` | 412 packages, 29 vulns (pre-existing, same as yesterday) |
| `npm run typecheck` | **Clean** — 0 errors |
| `npm test` | **31/31 pass** — 0 failed, 0 skipped |
| `npm run build` | **Build OK** |

**web/ and marketing/** not retested (no changes to main since yesterday). The web/ build remains broken with 54 TS errors and 17/36 failing tests, as documented in the July 24 summary.

---

## New Activity Tonight (since July 24 summary)

Two new branches were created after the July 24 integration review:

### 1. `nightly-competitor-research-code-review-tools`
- **Type**: Research only
- **File**: `docs/research/automated/2026-07-25-ai-code-review-explanation-tools.md`
- **Content**: Analysis of CodeRabbit, GitHub Copilot, Cursor, Sourcegraph Cody, What The Diff, and Anthropic Codex — focusing on how they handle developer *comprehension* of AI-generated code
- **Key finding**: No competitor addresses post-hoc comprehension scaffolding for AI-generated code. CodeRabbit's "Learnings" feature captures review preferences, not developer understanding. Unvibe's v2 overlay concept extraction remains genuinely unique.
- **Conflicts**: None (docs only)

### 2. `nightly-desktop-overlay-event-cleanup`
- **Type**: Code fix + docs
- **Files changed**: `app/src/preload/preload.ts` (+25/-5), `docs/automation/nightly/2026-07-25-summary.md`
- **Fix**: 5 IPC event listeners (`onReviewEvent`, `onAutocollapse`, `onSyncStatus`, `onShortcutFired`, `onDeviceAuth`) did not return cleanup functions, causing listener accumulation on React mount/unmount. Changed to use named listener wrappers with `ipcRenderer.removeListener()` cleanup pattern.
- **Verification**: typecheck clean, 31/31 tests pass, build ok
- **Conflicts**: **None** — `preload.ts` is untouched by any other night lab branch. This fix composes cleanly with all other pending changes.

---

## Branch Inventory (all 15 night lab branches)

| Branch | Type | Files | PR | Status |
|--------|------|-------|----|--------|
| **NEW** `nightly-competitor-research-code-review-tools` | Research | 1 doc | No | Not reviewed |
| **NEW** `nightly-desktop-overlay-event-cleanup` | Code fix | 1 code + 1 doc | No | Not reviewed |
| `nightly-backend-and-sync-audit-staging-docs` | Docs | 3 docs | No | Needs review |
| `nightly-competitor-research-desktop-overlay` | Research | 1 doc | No | Needs review |
| `nightly-competitor-research-desktop-overlay-comprehension` | Research | 1 doc | No | Needs review |
| `nightly-desktop-overlay-accessibility-poll-fix` | Code fix | 1 file | No | companion.tsx conflict |
| `nightly-desktop-overlay-overview` | Code + docs | 2 files | No | Superseded by window-guard |
| `nightly-desktop-overlay-window-guard` | Code + tests | 5 files | No | Most complete version |
| `nightly-product-design-accessibility-companion-a11y` | Code fix | 2 files | No | companion.tsx conflict |
| `nightly-product-design-and-accessibility-plan-page-a11y` | Code fix | 2 files | No | companion.tsx conflict |
| `nightly-product-design-and-accessibility-settings-escape` | Code fix | 2 files | No | companion.tsx conflict |
| `nightly-product-design-and-accessibility-sidebar-a11y` | Code fix | 1 file | No | companion.tsx conflict |
| `nightly-integration-review-and-morning-summary-20260723` | Docs | 1 doc | No | Superseded by v2 |
| `nightly-integration-review-and-morning-summary-20260723-v2` | Docs | 1 doc | No | Superseded by Jul 24 |
| `nightly-integration-review-and-morning-summary-20260724` | Docs | 1 doc | No | Superseded by this report |

---

## Overlap Analysis

### No new conflicts
The two new branches touch files not modified by any previous branch:
- `preload.ts` — only changed by `event-cleanup`
- `docs/research/automated/2026-07-25-ai-code-review-explanation-tools.md` — new file

### Preexisting conflicts (unchanged from yesterday)
1. **`companion.tsx`** — still has 5 conflicting change sets across 5 branches
2. **`main.ts`, `windows.ts`** — 3 branches modify overlapping code (fullProductBuildEnabled)
3. **`window-guard`** remains the most complete version for the main/windows changes

### Event-cleanup branch compatibility
The `preload.ts` fix is **fully compatible** with all pending changes. No existing branch touches the preload bridge. If `companion.tsx` merges later, the cleanup-aware event subscriptions in the renderer will correctly use the new listener API without changes.

---

## Problems Discovered

### Critical (unchanged from yesterday)
1. **web/ build broken** — `revokeToken` missing from Store interface. Sign-out throws at runtime. Prevents backend deployment.
2. **54 type errors in web/** — Interface/type misalignment, unchanged since yesterday.
3. **17/36 web tests fail** — Missing exports from `plans.ts` and missing Store methods.
4. **companion.tsx 5-way conflict** — Must be manually reconciled. Each branch changes overlapping JSX.

### New
5. **29 npm vulnerabilities in app/** — Pre-existing, but remains unresolved. Mostly high-severity transitive deps.

### Minor (documented in event-cleanup summary)
6. `settings.ts:144-148` — Silent persist failure on filesystem write errors
7. `backend.ts:48-53` — Empty auth header sent when not trial/full-product build
8. `main.ts:87-93` — macOS-specific `firstName()` call with fallback

---

## Research Completed Tonight

- **AI Code Review & Explanation Tools** (`docs/research/automated/2026-07-25-ai-code-review-explanation-tools.md`): Six products analyzed. Confirmed Unvibe's unique position in post-hoc code comprehension scaffolding. No competitor addresses structured learning from AI-generated code.

---

## Security & Privacy

- **No new vulnerabilities**. Event-cleanup fix does not touch network I/O, secret filtering, or credential handling.
- No Supabase keys in app/ renderer code (verified by event-cleanup branch's own audit).
- All app/ I/O remains behind contextIsolation + sandbox.

---

## Required Founder Decisions (unchanged from yesterday)

1. **`revokeToken` + `historyPage`**: Add to Store interface + MemoryStore + SupabaseStore? Unblocks sign-out, pagination. ~20-30 min fix.
2. **Companion.tsx reconciliation**: 5 change sets to merge manually. Order matters.
3. **`fullProductBuildEnabled` removal**: Accept the **window-guard** version (most complete).
4. **web/ type alignment**: Backfill `localDate`/`timezone`, `conceptsFamiliar`/`conceptsStrong`, `language`.
5. **Billing plans.ts exports**: 11 missing exports — regression or intentional?
6. **Night Lab permissions**: Still blocked from creating PRs — `createPullRequest` scope needed.

---

## Recommended Review Order

1. **`nightly-desktop-overlay-event-cleanup`** — Low-risk single-file fix, typechecked + tested, no conflicts
2. **`nightly-competitor-research-code-review-tools`** — Docs only, zero risk
3. **`nightly-backend-and-sync-audit-staging-docs`** — Docs only, understand web/ state first
4. **`nightly-competitor-research-desktop-overlay-comprehension`** + **`nightly-competitor-research-desktop-overlay`** — Research docs
5. **`nightly-desktop-overlay-window-guard`** — Most complete code change, has tests, platform-guarded
6. Remaining companion.tsx branches — After reconciliation decision

## Recommended Merge Order

1. **`nightly-desktop-overlay-event-cleanup`** — Merge immediately (low-risk, tested, no conflicts)
2. **`nightly-competitor-research-code-review-tools`** — Merge immediately (docs only)
3. **`nightly-backend-and-sync-audit-staging-docs`** — Merge immediately (docs only)
4. Remaining research branches — Merge immediately (docs only)
5. **`window-guard`** → then cherry-pick companion.tsx fixes in reconciled order
6. Close **`overview`** (superseded by window-guard)

---

## Suggested Next Daytime Task

1. **Merge the event-cleanup PR** (preload listener fix) — 5-minute review
2. **Fix web/ build**: Add `revokeToken` and `historyPage` to Store interface to unblock backend

---

## Night Lab Status Summary

- **15 total branches** created across all night lab runs
- **2 new branches** tonight: 1 code fix, 1 research report
- **0 PRs open** — all blocked by GITHUB_TOKEN permissions
- **1 code fix** ready for immediate merge (event-cleanup, no conflicts)
- **3 docs-only branches** ready for immediate merge
- **5 companion.tsx branches** awaiting reconciliation decision
- **No conflicts** between the new work and existing branches
- `app/` healthy: 31/31 tests pass, typecheck clean, build OK
- `web/` still broken: 54 TS errors, 17/36 tests fail, build fails
- All macOS behavior remains "unverified on Linux runner"
