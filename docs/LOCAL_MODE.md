# Local mode

BYOK is shipped: the user stores a provider key in the OS credential store and Unvibe calls that provider from the desktop.

Offline local-weights inference is not shipped. `features.localModels` is reserved. Do not name a specific local model in product copy.

When cloud is used, only the secret-filtered snippet leaves the machine.
