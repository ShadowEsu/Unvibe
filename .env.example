# Repository environment map — copy values into the owning service only. Never put secrets here.
# Desktop values belong in app/.env; backend values belong in web/.env.local or deployment secrets.
APP_ENV=development
WEB_BASE_URL=http://localhost:8787
DEEP_LINK_SCHEME=unvibe
