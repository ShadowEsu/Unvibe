# Unvibe design tokens

Live tokens used by the desktop renderers live in `app/src/renderer/shared/tokens.css`.

The app build prepends that file to Island, widget, and companion CSS. Do not add a second palette.
